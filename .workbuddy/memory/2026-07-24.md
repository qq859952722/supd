# 2026-07-24 工作记忆

## 任务：DEV-007/011/012 偏差转规格 + 本次变更运行状态测试

### 偏差转规格（用户决定：已决策项不再记偏差，直接改需求文档）
- `docs/需求规格说明_v1.5.md` 已改：
  - §2.3.1 history 路径：移除 SHA256 哈希，改用直接相对路径（DEV-007）
  - §2.2.3 actions：移除 `icon`/`enabled` 字段（DEV-012）
  - §2.2.3 triggers：由 list 改为 map（`on_demand`/`on_schedule`/`service_lifecycle`/`supd_lifecycle` 四键，`cron`/`event` 字段），含交叉引用与示例 meta.yaml（DEV-011）
- `docs/devlog/deviations.md`：移除 DEV-007/011/012 三块
- 同步：`docs/服务扩展开发指南.md`、`docs/devlog/session-notes.md`、`docs/devlog/notes/2026-07-24.md`、`docs/devlog/notes/2026-07-23.md`（仅更新口径说明，不再标注为 active 偏差）
- 未动项（用户指示"稍后讨论"）：测试覆盖率、技术债 M-03-001/M-04-001/TD-005

### 运行状态测试（本次三项变更）
1. tracker-updater 全局 Tracker + 动态 rpc-port：node 适配器 shim `tjs` + fetch 拦截 mock RPC。
   - 阶段 A：config 指定 rpc-port=19091 动态读取 ✅ + session-set default_trackers(5) ✅ + 409 CSRF 重试 ✅
   - 阶段 B：无 config 回退默认 9091 ✅
   - 脚本：`/tmp/tracker_test/runner.mjs`（纯 fetch 拦截，规避端口占用）
2. 进程树 DFS 递归：`internal/api/runtime_state_test.go::TestRuntime_ProcessTreeDFS` 收集 2 层进程树 5 进程 ✅
3. CPU 首次采样补偿：`TestRuntime_CPUFirstSampleCompensation` 首次采样 35.81%>0 ✅
- `go build ./...` ✅、`go vet ./internal/api/` ✅

### 踩坑
- node harness 阶段 B 初版用真实 HTTP mock 监听 9091，但端口被占用 EADDRINUSE；改为纯 `fetch` 拦截并捕获 run.js 实际使用的 RPC 端口来断言，更确定。

### 文档/决策清理（2026-07-24 后续）
- **TD-005（useLongPolling hook）已关闭**：前端重构后长轮询由 `web/src/lib/api-client.ts` 的 `apiLongPoll<T>` 实现，原 hook 不存在；已从 blockers.md / session-notes.md 移除。
- **M-03-001（yaml v4 rc）重新定性为有意技术决策（非技术债）**：internal/api 直接用 `go.yaml.in/yaml/v4 v4.0.0-rc.6`。已写入需求规格说明 v1.5 附录A.A.5，并从 blockers.md / session-notes.md / 2026-07-23.md 债务清单移除。
- **M-04-001（superviseService 圈复杂度 43）/ TD-003（superviseService 重复）仍为活债务**，未动。
- M-04-001 重构方案评估：方案详尽但有3处行为回归硬伤（OnFailure ctx、readiness ctx、checkReadiness error 丢弃），修补后可落地。
- 已将评估发现的全部修复应用到 `tmp/M-04-001-supervisor-refactor-plan.md` v2：3个硬伤（handleProcessExit加ctx参数+OnFailure用ctx、SpawnNextSupervisor返回context.Context+RunReadiness用readinessCtx、RunReadiness返回error+newProc.Done处理）、3个遗漏（D2标注有意策略变更、Source字段区分cli/api、DecideRestart签名直接修正）。
- M-04-001/TD-003 重构已实施完成：新建 supervisor.go（SupervisorCallbacks + RunSupervisor + 4 辅助函数）+ supervisor_test.go（T1-T5）+ bootstrap/service_operator 改为薄包装 + rebuildLogger 提取 + T6 端到端测试。CC 从 43→3/32→6，最高19。全部测试通过，M-04-001/TD-003 关闭。

### M-04-001/TD-003 审计 + 运行测试全通过 + v0.0.21 发布（本会话最终）
- 审计：git show HEAD 逐行比对，3 ctx/error 硬伤 + P1 均正确修复；gocyclo bootstrap 43→3 / service_operator 32→6 / 最高单函数 19；-race 通过；无新 bug。
- 运行测试 7 场景 A–G 全 PASS（真实实例 /tmp/supd-rt v0.0.21）。关键：fd_notify 重启就绪经 200ms 细粒度轮询确认 OK（早期 3s 轮询遗漏短暂 ready 属轮询粒度问题，非缺陷）；audit-fd2(8s)/audit-backoff(退避中断) 为专属验证服务。
- 发布 v0.0.21（PATCH）。README v0.0.19→v0.0.21（跳过未 bump 的 v0.0.20）。tag v0.0.21 已推送 origin/main + tags。
- 测试服务放在 /tmp/supd-rt（仓库外，不污染）；运行时测试用服务/扩展写在 services/ 下，非仓库文件。
