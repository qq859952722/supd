# 2026-07-24 工作记忆

## 任务：DEV-007/011/012 偏差转规格 + 本次变更运行状态测试

### 偏差转规格（用户决定：已决策项不再记偏差，直接改需求文档）
- `docs/需求规格说明_v1.5.md` 已改：
  - §2.3.1 history 路径：移除 SHA256 哈希，改用直接相对路径（DEV-007）
  - §2.2.3 actions：移除 `icon`/`enabled` 字段（DEV-012）
  - §2.2.3 triggers：由 list 改为 map（`on_demand`/`on_schedule`/`service_lifecycle`/`supd_lifecycle` 四键，`cron`/`event` 字段），含交叉引用与示例 meta.yaml（DEV-011）
- `docs/devlog/deviations.md`：移除 DEV-007/011/012 三块
- 同步：`docs/服务扩展开发指南.md`、`docs/devlog/session-notes.md`、`docs/devlog/notes/2026-07-24.md`、`docs/devlog/notes/2026-07-23.md`（仅更新口径说明，不再标注为 active 偏差）
- 未动项（用户指示"稍后讨论"）：测试覆盖率、技术债 M-03-001/M-04-001/TD-005

### 运行状态测试（本次三项变更）
1. tracker-updater 全局 Tracker + 动态 rpc-port：node 适配器 shim `tjs` + fetch 拦截 mock RPC。
   - 阶段 A：config 指定 rpc-port=19091 动态读取 ✅ + session-set default_trackers(5) ✅ + 409 CSRF 重试 ✅
   - 阶段 B：无 config 回退默认 9091 ✅
   - 脚本：`/tmp/tracker_test/runner.mjs`（纯 fetch 拦截，规避端口占用）
2. 进程树 DFS 递归：`internal/api/runtime_state_test.go::TestRuntime_ProcessTreeDFS` 收集 2 层进程树 5 进程 ✅
3. CPU 首次采样补偿：`TestRuntime_CPUFirstSampleCompensation` 首次采样 35.81%>0 ✅
- `go build ./...` ✅、`go vet ./internal/api/` ✅

### 踩坑
- node harness 阶段 B 初版用真实 HTTP mock 监听 9091，但端口被占用 EADDRINUSE；改为纯 `fetch` 拦截并捕获 run.js 实际使用的 RPC 端口来断言，更确定。

### 文档/决策清理（2026-07-24 后续）
- **TD-005（useLongPolling hook）已关闭**：前端重构后长轮询由 `web/src/lib/api-client.ts` 的 `apiLongPoll<T>` 实现，原 hook 不存在；已从 blockers.md / session-notes.md 移除。
- **M-03-001（yaml v4 rc）重新定性为有意技术决策（非技术债）**：internal/api 直接用 `go.yaml.in/yaml/v4 v4.0.0-rc.6`。已写入需求规格说明 v1.5 附录A.A.5，并从 blockers.md / session-notes.md / 2026-07-23.md 债务清单移除。
- **M-04-001（superviseService 圈复杂度 43）/ TD-003（superviseService 重复）仍为活债务**，未动。
- M-04-001 重构方案评估：方案详尽但有3处行为回归硬伤（OnFailure ctx、readiness ctx、checkReadiness error 丢弃），修补后可落地。
- 已将评估发现的全部修复应用到 `tmp/M-04-001-supervisor-refactor-plan.md` v2：3个硬伤（handleProcessExit加ctx参数+OnFailure用ctx、SpawnNextSupervisor返回context.Context+RunReadiness用readinessCtx、RunReadiness返回error+newProc.Done处理）、3个遗漏（D2标注有意策略变更、Source字段区分cli/api、DecideRestart签名直接修正）。
- M-04-001/TD-003 重构已实施完成：新建 supervisor.go（SupervisorCallbacks + RunSupervisor + 4 辅助函数）+ supervisor_test.go（T1-T5）+ bootstrap/service_operator 改为薄包装 + rebuildLogger 提取 + T6 端到端测试。CC 从 43→3/32→6，最高19。全部测试通过，M-04-001/TD-003 关闭。

### M-04-001/TD-003 审计 + 运行测试全通过 + v0.0.21 发布（本会话最终）
- 审计：git show HEAD 逐行比对，3 ctx/error 硬伤 + P1 均正确修复；gocyclo bootstrap 43→3 / service_operator 32→6 / 最高单函数 19；-race 通过；无新 bug。
- 运行测试 7 场景 A–G 全 PASS（真实实例 /tmp/supd-rt v0.0.21）。关键：fd_notify 重启就绪经 200ms 细粒度轮询确认 OK（早期 3s 轮询遗漏短暂 ready 属轮询粒度问题，非缺陷）；audit-fd2(8s)/audit-backoff(退避中断) 为专属验证服务。
- 发布 v0.0.21（PATCH）。README v0.0.19→v0.0.21（跳过未 bump 的 v0.0.20）。tag v0.0.21 已推送 origin/main + tags。
- 测试服务放在 /tmp/supd-rt（仓库外，不污染）；运行时测试用服务/扩展写在 services/ 下，非仓库文件。

## 任务：测试覆盖率提升计划执行（P0/P1/P2）— 闭环 L-01-001 / L-04-001

### 目标与结果
- L-01-001（api）：43.3% → **68.0%**（目标 ≥65% 达成）。
- P1（cli）：50% → **66.1%**（目标 ≥65% 达成）。
- L-04-001（前端）：绿地 → Vitest 4 框架 + **38 例** util 单测全过。
- 质量水位 97.44 → **97.84 / 100**（回收 -0.400）。

### Hermetic 测试模式（关键经验）
- Go：`NewServer(cfg)` + 直接注入 `server.*` 字段（fake provider 嵌入接口仅覆写所需方法）+ httptest 驱动；`getAPIClient` 是包级 `var`，cli 命令测试可注入 httptest 服务端。
- 前端：Vitest node 环境，`vi.stubGlobal('window', {location:{origin}})` + `vi.stubGlobal('fetch', vi.fn())` + `vi.mock('@/components/ui/Toast')`；`@tanstack/react-query` 用 `vi.mock` 子类捕获构造参数以测 retry 分支。

### 踩坑（已解决）
- 扩展导入缺失 `file` 字段 → 400（非 413；413 仅对应 `ParseMultipartForm` 失败）。
- `handleRunExtension` 对 `nil` body 调 `json.NewDecoder(r.Body).Decode` 会 panic → 传 `[]byte("")`。
- cli JSON 解码类调用需 handler 返回真实 JSON body（空 body→EOF 报错）。
- `buildStopConfigs` 对 `result.Discovery==nil` 无保护，但生产路径 Discovery 总由 bootstrap 赋值，非可达；按维护期约束未改生产代码。

### 验证全绿
- 后端：`go build ./...` ✅ `go vet ./...` ✅ `go test ./... -count=1` ✅
- 前端：`pnpm test` 38 例 ✅；`pnpm build` ✅

### 未提交
- 新增测试文件均为 untracked；v0.0.21（commit 5a82e07 + tag）仍本地未推送（用户"暂不推送"决策）。

## 任务：测试覆盖率提升计划 P3（前端主链路集成 + CI 接入）

### 成果
- api-client.ts：67.21% → **90.16%**（扩至 22 例：4 个 REST 封装 + handleResponse 全分支）。
- http-probe.ts：0% → **87.27%**（新增 9 例，jsdom + @testing-library/react renderHook；覆盖 useHTTPProbe hook + sortAndLimitPorts/invalidatePortCache）。
- 前端单测 38 → **60 例**；lib 总覆盖 **91.03%**。
- CI：`.github/workflows/coverage.yml`（go/web artifact 上传，非阻断、不设阈值）；`Makefile` 加 `cover`/`cover-web`；`.gitignore` 补 `cover.html`/`web/coverage/`。
- `make cover` 后端总覆盖 **73.6%**。

### 关键经验 / 坑
- **前端测试构建修正**：`web/tsconfig.json` 的 `tsc -b` 会把 `*.test.ts` 纳入生产构建类型检查；项目开 `noUncheckedIndexedAccess` + `ApiError.code` 必填，导致测试类型错误使 `pnpm build` 失败。修正：tsconfig 加 `exclude: ["src/**/*.test.ts","src/**/*.test.tsx"]`（测试仍由 vitest 执行）。P2 曾误报 build 通过，本次纠正。
- http-probe hook 测试用 `// @vitest-environment jsdom` + `renderHook`；`probePort` 用 `window.location.hostname` 拼 URL，fetch 仅判断 resolve/reject，mock 返回 `{}` 即可（无需真实 Response）。
- 204 状态构造须 `new Response(null, { status: 204 })`，undici 拒绝 204 带 body。
- api-client 测试：`vi.stubGlobal('window', {location:{origin}})` + `vi.stubGlobal('fetch', vi.fn())` + `vi.mock('@/components/ui/Toast')`，断言 `toast.error` 调用。

### 验证全绿
- 后端：`go build/vet/test ./...` ✅；`make cover` 73.6% ✅
- 前端：`pnpm test` 60 例 ✅；`pnpm build` ✅

### 未提交
- P3 新增/修改（api-client.test.ts 扩写、http-probe.test.ts、tsconfig exclude、package.json devDeps jsdom/@testing-library/*、coverage.yml、Makefile、.gitignore）均为 untracked / 未提交；v0.0.21 仍本地未推送。

## 任务：测试覆盖率提升计划 P4（Go 核心包攻坚）

### 成果（真实测量，非估算）
- 6 个新增测试文件共 **35 例**：`core/state_machine_test.go`(3) `core/pidfile_test.go`(7) `extension/cron_scheduler_gaps_test.go`(4) `extension/trigger_cron_gaps_test.go`(4) `extension/dispatcher_gaps_test.go`(2) `api/service_operator_test.go`(15)。
- 后端总覆盖率 **73.6% → 74.9%**（+1.3pp，实测 6787/9065 stmts）。
- 包级：core 81.6→84.6(+2.9) / extension 76.4→80.4(+3.9) / api 68.0→68.6(+0.6)。
- 逐文件真实增量（移走 P4 测试文件测基线、还原后比对）：state_machine 74.5→93.6(+19.1)、pidfile 23.7→64.4(+40.7)、cron_scheduler 40.2→66.7(+26.4)、trigger_cron 51.2→70.9(+19.8)、dispatcher 73.5→77.0(+3.4)、service_operator 44.5→52.5(+8.0)、task_manager 50.0→50.0(跳过，已充分覆盖)。
- 全部 hermetic，未改生产代码；`go test ./...` 全绿（core 51.9s / extension 34.3s / api 4.7s 无 FAIL）。

### 关键坑（已校正，务必记牢）
- **覆盖率数字必须实测**：凭印象写的逐文件增量（service_operator 63.4%、cron_scheduler 89.7% 等）与实际不符。正确做法 = 把新增测试文件移走跑基线、还原跑 P4 后、逐文件 diff coverage profile。
- `cron_scheduler.GetNextRun` 仅在 `Start()` 后算 `entry.Next`；测试须先 `sched.Start()` + `defer sched.Stop(ctx)`，断言 `!next.IsZero()`。
- `writeServiceLog` fallback 路径 = `<LogDir>/services/<name>/current`（非 `*.log`），断言用 `os.Stat` 精确路径。
- `EventPublisher.Publish` 签名 = `Publish(string, any)`（payload 是 `any`，不是 `map[string]any`）。
- `state_test.go` 已极完备，**勿重写重复**；P4 仅补 publisher 分支。
- ⚠️ **mv 事故**：`mv a b c d/`（多源+多目标目录）会把所有源（含目录）移进最后一个目录参数 → `internal/core`、`internal/extension` 被误移进 `internal/api/`。已用逐个单源单目标 `mv` 还原并验证 `go build`/`go test` 全绿。**今后 mv 一律一次一个源一个目标**。

### 未提交
- 6 个新增 Go 测试文件 untracked；v0.0.21 仍本地未推送（用户"暂不推送"）。
