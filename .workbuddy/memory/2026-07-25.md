# 2026-07-25 工作记录

## 优化 192.168.31.188:7979 的两个下载服务（supd 实例，workdir=/etc/supd）
- 访问：SSH `root@192.168.31.188 -p 2222`（dropbear-ssh，开发机免密）。`/etc/supd` 才是 :7979 真正读取的工作目录（本机 /tmp/test-workdir 是无关沙盒）。
- **qBittorrent**：service.yaml 加 `user: nobody`（原以 root 运行）；新增 `pre-start-fixperms` 扩展（meta.yaml `run_as: root` + `service_lifecycle.pre_start`）→ run.sh 递归 chown 工作目录为 nobody 并创建/修复 `/Downloads/Qdown`、`/Downloads/inQdown`；qBittorrent.conf 加 `[Downloads]` SavePath=/Downloads/Qdown/、TempPath=/Downloads/inQdown/（用 stop→编辑→start 避免被退出重写覆盖）。
- **Transmission**：本就 `user: nobody`；新增同名 `pre-start-fixperms` 扩展修复 `/etc/supd/services/transmission/config` 与 `/Downloads/Tdown`、`/Downloads/inTdown`；settings.json 设 download-dir=/Downloads/Tdown、incomplete-dir=/Downloads/inTdown、incomplete-dir-enabled=true（同样 stop→编辑→start）。
- 验证：两守护进程均 `nobody` 运行；pre_start 各触发 3 次/2 次全部 success；4 个下载目录均为 nobody:nobody。
- **trackerlist 认证答疑**：transmission RPC `rpc-authentication-required:false`、`rpc-whitelist:127.0.0.1,::1`。tracker-updater 脚本走 CSRF `X-Transmission-Session-Id`（409 重试）认证，无账号密码，经 127.0.0.1 连接（开发机直连会 403）。

## 端口交叉归属问题排查（transmission 显示 8080）
- **根因**：188 容器内 `yama ptrace_scope=1` → `readlink /proc/<pid>/fd/*` 对 `nobody` 进程返回 Permission denied（即使 root 也被拒）→ supd inode 精确匹配路径完全失败 → 走 UID 降级路径 → **qbittorrent-nox (pid=271, UID=65534) 和 transmission-daemon (pid=307, UID=65534) 都以 nobody 运行** → UID 匹配把所有 UID=65534 的端口归到两个服务上 → 8080（qbittorrent WebUI）被错误归属到 transmission。
- **真正监听 8080 的进程**：qbittorrent-nox `--webui-port=8080`（pid=271）。transmission 的 RPC 端口是 9091，peer 端口是 51413。
- **代码位置**：`port_collector.go` L52-95 `collectProcessPorts()` → L63-79 UID 降级分支 → `matchNetSocketsByUID()` 按 fields[7]（uid列）匹配 `/proc/net/{proto}`。
- **影响范围**：同一 UID 的所有服务端口列表互相串扰（nobody 用户的 qbittorrent/transmission 端口全部混在一起）。

## 端口交叉归属修复：UID 降级路径添加 cmdline 交叉验证
- **改动**：`collectProcessPorts(pid int)` → `collectProcessPorts(pid int, cmdPattern string)`
- **新增函数**：`collectInodesByCmdlineUID(uids, cmdPattern)` — 扫描 `/proc` 找所有 UID 匹合且 cmdline 包含 cmdPattern 基础名的进程，对其再次尝试 `collectSocketInodes`，缩小端口归属范围
- **新增辅助**：`cmdPatternFromConfig(cfg)` — 从 `ServiceConfig.Command[0]` 提取命令模式
- **降级层级**：
  1. 主路径：inode 精确匹配（readlink /proc/<pid>/fd → socket inode）
  2. 降级1：UID + cmdline 交叉验证 → 对 cmdline 匹合的进程重试 inode → 如果有 inode 结果，精确匹配
  3. 降级2：纯 UID 匹配（最终兜底，日志 Warn 级别提示可能串扰）
- **修改文件**：port_collector.go、service_handler.go、resource_handler.go、port_collector_test.go
- **测试**：新增 `TestCmdPatternFromConfig`（4 子场景）、`TestCollectInodesByCmdlineUID_Basic`（3 边界条件）；全量 go build/vet/test 通过

## 关键踩坑
- 下载目录配置会被进程退出时重写覆盖 → 必须 stop→改文件→start，不能直接 restart。
- :7979 不在本机；之前 memory 误记 workdir 为 /tmp/test-workdir，已更正 MEMORY.md。

## 全局环境变量编辑弹窗（EnvEditor Dialog）
- **改动**：Settings 页面的全局 EnvEditor 从内嵌卡片改为弹窗（Dialog）模式
- **新增组件**：`EnvEditorDialog`（点击按钮弹出 Dialog）+ `EnvEditorCard`（保留兼容）+ `YamlFormatHint`（YAML 格式参考提示）
- **弹窗内容**：YAML 格式参考说明（env: { KEY: { value, enabled?, hint? } }）+ 表格化可视化编辑器（Key/Value/Hint/Enabled 列）+ 敏感字段切换 + 校验（空 key、重复 key）
- **Settings 页面**：env 区域改为 Card 卡片（标题 + 描述 + 格式说明文字）+ 右侧"编辑环境变量"按钮触发弹窗
- **修改文件**：EnvEditor.tsx（重写）、Settings.tsx（导入替换 + env 区域改造）
- **前端构建通过**

## 安装项目级技能 supd-service-extension-dev
- **安全审计**（2026-07-25）：当前环境 `skills-security-check` 技能不可用，手动审计 SKILL.md + `scripts/validate_dev.py`/`pack_dev.py`。结论 **P2 安全**：无网络外联、无越权文件写/删、仅当目标目录含 config.yaml 且 PATH 有 supd 二进制时才调 `supd validate`（5s 超时）。`references/` 与 `examples/` 均为文档/示例，不自动执行。
- **安装位置**：`.workbuddy/skills/supd-service-extension-dev`（项目级，27 文件完整复制）。源 `.trae/skills/supd-service-extension-dev` 保留未删除。
- 用途：supd 服务/扩展一条龙开发（service.yaml/meta.yaml/run.sh/run.js 规格、references 6 手册、examples 9 示例、validate+pack 脚本）。

## 修复 transmission tracker-updater 扩展（协议过滤 bug）
- **根因**：188 实例 `/etc/supd/services/transmission/extensions/tracker-updater/run.js` 从 `adysec/tracker` 的 `trackers_best.txt` 拉取后**原样**写入 Transmission `default_trackers`，未过滤不支持协议。Transmission 仅支持 `http://`/`https://`/`udp://`（官方 Tracker Communication 文档确认），**不支持** `ws://`/`wss://`（WebTorrent/WebSocket）、`i2p://`。数据源含 `wss://` 条目，旧版会把无效 tracker 塞入导致报错/被忽略。
- **修复**：v1.1.0。新增协议白名单 `SUPPORTED_SCHEMES=['http://','https://','udp://']` + 小写 URL 去重 + 多格式兼容拆分（`\r?\n` / 空格 / 逗号）+ 跳过 `#` 注释行。仅保留支持协议。
- **验证（188 实跑）**：`SUPD_ACTION=update-trackers SUPD_SERVICE_DIR=/etc/supd/services/transmission /usr/local/bin/tjs run.js` → 数据源 313 个 → 保留 306 个（http×119 https×29 udp×158），**跳过 wss×7**，RPC session-set 成功。
- **部署**：tar 管道推到 `/etc/supd/services/transmission/extensions/tracker-updater/`（run.js 0755, meta.yaml 1.1.0），supd fsnotify 热重载生效。
- **本地归档**：`tmp/tracker-updater/{run.js,meta.yaml}`；`validate_dev.py` 校验全 PASS。

## 修正 tracker-updater v1.1.1（保留空行分组）
- **用户反馈**：v1.1.0 的 `buildTrackerList` 用 `.split(/\r?\n/).flatMap(line=>line.split(/[\s,]+/)).filter(s=>s.length>0)`，把空行当空条目过滤掉——但**空行是列表分组格式的一部分，必须保留**。
- **修正**：改为逐行处理（`.split(/\r?\n/)` 遍历），空行 `trim().length===0` 时**原样 push 到输出**，不参与过滤/去重；注释行 `#` 原样保留；仅对非空行做协议白名单过滤(wss/i2p 等删除并计数)与大小写不敏感去重。`rawCount` 改用新增 `countRawEntries()`（非空非注释计数）。`filteredList = keptLines.join('\n')`。
- **验证**：构造含空行/注释/wss/重复/逗号分隔样例 → 空行结构保留、wss×1 过滤、去重生效；真实 `trackers_best.txt`（313 行）本地跑 → 过滤后 307 行含 1 空行 + 306 非空，skipped wss×7。
- **部署**：v1.1.1 已 tar 管道推到 `/etc/supd/services/transmission/extensions/tracker-updater/`，188 实跑 RPC session-set 成功（保留 306：http×119 https×29 udp×158，跳过 wss×7）。

## 在 188 部署 AdGuardHome 服务（supd 托管）
- **版本**：v0.107.78 linux_amd64（github release）。188 是 x86_64 / Alpine 3.20（musl）。实测二进制为**静态链接**，ldd 报 "Not a valid dynamic program" 但 `--version` 正常输出 → 无需 glibc/gcompat，可直接在 musl 上运行。
- **部署路径**：`/etc/supd/services/adguardhome/`，含 `AdGuardHome`（二进制 0755）、`AdGuardHome.yaml`（配置文件，AGH 首次运行自动补全 schema_version 34 + 自建 `data/` 目录）、`service.yaml`（autostart:true，command `[./AdGuardHome]`，http_check 就绪检测 `http://127.0.0.1:8088/`）。
- **端口决策（重要）**：188 上 **3000 已被某未知 Web 服务占用**（页面像 AGH 但无进程，疑似历史残留；53 空闲）。为不破坏现有服务，新实例管理端口改用 **8088**（已确认空闲），DNS 仍用 **53/UDP**。
- **管理员密码**：v0.107.78 无 `--hash-password` CLI 选项。先用明文 `admin` 启动 → AGH 未自动 bcrypt 化 → 装 `apache2-utils` 用 `htpasswd -bnBC 10` 生成 `$2y$10$...` bcrypt 哈希替换 `users[].password`，`--check-config` 通过；重启服务生效。
- **配置坑（重要）**：该版本 `bootstrap_dns` 在 `--check-config` 解析时**期望单个字符串**（写列表会报 `cannot construct !!seq into string`），但 AGH 自动迁移后内部会写成列表（运行正常）。最终保留 AGH 自己补全的列表写法。
- **验证**：supd API `POST /services/adguardhome/start` → 状态 ready（pid 351→重启后 416）；8088 TCP + 53 UDP 监听正常；`dig @127.0.0.1 example.com/google.com` 解析成功；Web `/` 返回 302。
- **开机自启**：`autostart:true` 已落盘，supd 启动时会自动拉起（无需实测重启盒子）。新增服务目录在 supd 运行后出现的，autostart 不立即生效，需手动 start 一次；之后由 supd 框架保证重启自启。
- **本地归档**：`tmp/adguardhome/{service.yaml, AdGuardHome.yaml}`（最终版含 bcrypt 密码）。
