# 2026-07-25 工作日志

## supd 代码审计（续，A–O 全类完成）

从 K 类继续，完成 K/L/M/N/O 五类独立报告 + 最终汇总 `tmp/审计报告.md`。P/Q/A–O 共 15 类全部完成。

### 本日产出报告（tmp/）
- `audit_results_K_config.md`：K 4.0/4.0（配置校验解析期拦截，4 子项全过）
- `audit_results_L_test.md`：L 3.7/4.0（race 零竞态；50/5 长轮询限制器缺单测 L-02-001 🟡；三模式认证缺集成测试 L-04-001 🟡）
- `audit_results_M_quality.md`：M 4.9/5.0（go vet 0 警告；staticcheck 仅已知死代码；errcheck 真实项🔵；govulncheck/pnpm audit 0 漏洞；gocyclo 复杂度合理；前端 630KB chunk 🔵、缺 ESLint 🔵）
- `audit_results_N_e2e.md`：N 5.0/5.0（真实服务 e2e：生命周期/坏命令 failed 可读/扩展 2s 超时精确/热重载/长轮询第6连接 503 拒绝全部通过）
- `audit_results_O_arch.md`：O 4.9/5.0（依赖自顶向下无环；错误/日志/命名统一；13 规格数值全常量化，run.go:494 魔法数字 10 🟡）
- `审计报告.md`：汇总，**综合 77.5/79 ≈ 98.1%**，🔴0/🟠0/🟡5

### 关键验证（真实运行）
- `go test -race -count=10 ./...` → EXIT=0，0 DATA RACE（core 520s/extension 344s/api 147s 等）
- `govulncheck ./...` → 0 影响本代码漏洞；`pnpm audit` → 0 漏洞
- e2e 启服务 `./cmd/supd`，长轮询端点 `/api/events`，限制器 6th 连接 503 SERVICE_BUSY 验证生效

### 注意事项
- e2e 结束已停服务、删临时扩展/服务，`git status` 无污染。
- 工具 install：errcheck/govulncheck/gocyclo 需 `go install`，已装到 $HOME/go/bin。

## supd 审计修复 + 新增代码审计 + 运行状态测试方案（第二阶段）

依据 `tmp/审计报告.md` 的 5 个 🟡 缺陷执行修复（仅最小侵入、未做重构/未加功能）：

### 修复项（5 项全做）
1. **I-01-001**：删除 `internal/api/resource_collector.go` 死代码 `findChildProcesses`（原 319–363 行）+ 修正 :168 注释引用。
2. **I-01-002**：删除 `internal/api/task_handler_coverage_test.go` 未用 `clearErr` 字段（注：`service_ops_coverage_test.go` 同名 `clearErr` 为另一结构体且被使用，未动）。
3. **L-02-001 + N-05-001**：`internal/api/middleware_test.go` 新增 `TestLongPollLimiter_AcquireRelease`（直接测 Acquire/Release）+ `TestLongPollLimiterGlobalLimit50`（全局 50 上限，补 N-05-001）+ strconv 导入。
4. **L-04-001**：`internal/api/server_test.go` 新增 `TestAuthModes_RequestLevel`（挂载 `api.Server` 三模式 + `/api/health` 绕过，含 local_skip 远程/loopback 分流）+ config/errors 导入。
5. **O-05-001**：`internal/cli/run.go:494,497` 魔法数字 `grace=10`/`timeout=60` 改为 `config.DefaultStopGraceSeconds`/`config.DefaultStopTimeoutSeconds`（值不变）。

### 新增代码审计（`tmp/audit_newcode.md`）
- `go build ./...` / `go vet ./...` / `staticcheck ./internal/api/... ./internal/cli/...` 全 0；staticcheck 不再报 U1000（死代码确删）。
- errcheck 仅命中历史既有测试模式，新增代码 0 新项。
- `go test ./internal/api/... ./internal/cli/...` 全 ok；3 个新用例逐条 PASS（日志佐证 health 绕过/401/500 行为正确）。

### 运行状态测试方案（`tmp/运行状态测试方案.md`）
- 详细可执行的运行期测试战役：7 状态/11 转换基准、重启引擎、依赖编排、停止流、扩展 4 策略+timeout+cron、热重载、事件/长轮询、认证三模式、资源监控、API 安全、前端回归、边界异常，含可直跑 curl 脚本与验收标准。
- 前端 🔵 改进项（Monaco Schema/虚拟滚动/错误详情/ESLint/chunk）与重构/依赖升级按维护期约束留作独立迭代，未在本轮执行。

### 结论
- 修复 0 回归、0 新缺陷；仓库仅 5 个文件预期改动，无临时产物污染。
