# supd 开发日志压缩归档（2026-07-21 ~ 2026-09-01）

> 本档案为历史会话日志的压缩归档，**仅保留 supd 仓库源码/文档调整相关内容**。
> 对远程服务器（188/190）的服务部署、目录调整、SSH 运维等非源码操作已按用户要求清理，不再保留。
> 主索引：`../session-notes.md`。核心机制备忘见 `core-mechanisms.md`。

---

## 2026-07-21

- **Docker + tjs 集成与 CI**：tjs 静态二进制集成 Dockerfile（`/usr/local/bin/tjs-bin` + 包装脚本 + runtimes 软链接）；GitHub Actions 双架构原生编译（amd64/arm64 runner）+ 多阶段 Docker + multi-arch manifest 推 GHCR。文件：`Dockerfile`、`.github/workflows/build-push.yml`、`release.yml`。
- **v0.0.1 发布**：CI 修复（pnpm 固定 10、overrides 移 package.json、新建 `.dockerignore` 508MB→30MB）；README 完善。
- **仓库清理**：删 41MB 过程性文件；git 跟踪移除 `web/node_modules`（5213 文件）与 `web/dist`；`.gitignore` 修正；重建仓库（22 commit→1，跟踪文件 5562→330）。
- **script readiness bug**：`readiness_script.go` 未设 `cmd.Dir`，相对路径脚本 exit 127。修复：scriptChecker 增加 dir 字段，`NewReadinessChecker(cfg, dir)` 加 dir 参数，3 处调用点传 workdir。
- **v0.0.2**：`system_provider.go` 硬编码版本改为 `CoreSystemProvider.Version` 由 CLI 注入。
- **user 字段接入进程启动（重大）**：新建 `internal/identity`（LookupUserGroups/BuildCredential）+ `internal/core/credential.go`（ResolveServiceCredential/StartServiceProcess）；4 处 StartProcess 改 StartServiceProcess；dispatcher 传播服务 user；语义差异：服务严格拒绝（422）、扩展宽松警告。审计修复：全局扩展被 service_lifecycle 触发时不得继承服务 user。

## 2026-07-22

- **tjs 默认配置 + Docker 工具集**：`init.go` 模板 runtimes 含 `tjs: /usr/local/bin/tjs`；Dockerfile Stage 3 集成 26 个 Alpine 包；新增 auto-create-users 全局扩展（默认禁用，ALLID 驱动）。
- **v0.0.3 + 升级指南**：新建 `version-upgrade-guide.md`（ldflags 注入链路 + 6 步流程）。
- **Skill 审计修复 5 处**：run_as 值域、cron 5 段、args 移除、runtime 可选、DEV-005 完全修复。
- **服务 env.yaml 加载 BUG（重大）**：bootstrap.go 两处用 `os.Environ()` 未加载服务级 env.yaml。新建 `internal/core/service_env.go`（buildServiceProcessEnv 合并 3 层：os.Environ → 全局 env 文件 → 服务 env.yaml）+ 7 测试。
- **3 项规格偏差修复**：① script readiness 未继承服务 env（`NewReadinessChecker(cfg, dir, env)` 3 参数）；② on_failure 阶段 `SUPD_SERVICE_PID` 未注入（`OnFailure` 加 servicePID）；③ `CronScheduler.Stop(ctx)` 无超时。
- **前端 env 展示修复**：新建 `web/src/lib/env-yaml.ts` 共享工具；ServiceDetail/ExtensionDetail/两个 EnvEditor 统一。
- **v0.0.6 前 3 个 env BUG**：config/env.go YAML tag 缺 omitempty；parseEnvYaml 硬编码 indent===2；引号内 `#` 截断（stripYamlComment 跟踪引号状态）。
- **Dropbear SSH 集成**（第 3 版）：autostart:false + run.sh 认证模式自动选择 + env.yaml（SSH_PUBLIC_KEY/DROPBEAR_PORT）。

## 2026-07-23

- **全面审计 97.44 分**：go test -race 913+ 测试零竞态；审计报告 tmp/审计报告.md；扣分项 L-01-001（api 覆盖率 41.9%）/ L-04-001（缺 e2e）/ M-04-001（superviseService 圈复杂度 43）。
- **Skill 深度重构**：SKILL.md 926 行→101 行；建立 references/ 模块化参考库（新增 05_env_spec.md）；validate_dev.py 升能。
- **env 编辑器统一**：服务 EnvEditor 重写（对齐扩展风格）；扩展 EnvTab 增加"继承 + 最终生效"区块；后端 extension_handler.go 增加 Service 字段与 EnvPath 推导。
- **Skill 独立性强化**：去除对 AGENTS.md/session-notes 等外部文件引用；examples 改通用名（qbittorrent 硬编码→SERVICE_PORT 环境变量注入）；validate_dev.py 去层级硬编码。
- **retry_on_failure 补全（规格 §2.2.3 dead code 接入）**：config 增加 RetryOnFailureConfig；CronScheduler.SetCronTrigger/AddJob 加 retryCfg；CronTrigger.HandleResult 重试链。
- **RestartEngine 热重载修复**：新增 `SyncConfigFrom`（原地更新配置保留 retries）+ `UpdateRestartEngines`；`Policy()` 补锁（B-04-001）。
- **重试链审计修复**：executeRetry 回调 HandleResult（重试链断裂）；ClearRetryState 取消待执行重试 + 清 retryAttempts；retryCtx 锁内读取。
- **HTTP 访问日志 slog 化**：新增 accessLogMiddleware（method/path/status/bytes/duration_ms/request_id）；`--log-level` CLI BUG：BootstrapConfig 增加 LogLevel 字段（bootstrap.Run 重新加载 config 会丢弃 run.go 覆写）。
- **v0.0.12 tjs 验证**：镜像内 tjs v26.6.0 全通过；`RunExtensionRequest` JSON 字段是 `action`（非 action_id）；**tjs `arrayBuffer()` 大文件（>10MB）死锁**——必须用 ReadableStream 流式读取（06_tjs_runtime_guide.md 5.2/7.5 坑点）。
- **v0.0.13**：前端 5 处硬编码 127.0.0.1 → `window.location.hostname`（http-probe.ts、ServiceTable/Card/Overview、buildPortUrl）。
- **v0.0.14**：discovery 新增 `isBackupDir` 过滤 `.bak` 备份目录（三处扫描）；前端扩展表单 timeout_seconds clamp [1,1800]。
- **v0.0.15**：UpdateExtension 写文件后调用 `refreshDiscoveryMeta` 立即更新 Discovery 缓存（原 watcher 500ms 防抖窗口内 GET 旧值）。
- **v0.0.16 时区**：run.go 设置 time.Local（TZ env 默认 Asia/Shanghai）；Dockerfile `ENV TZ=Asia/Shanghai`。
- **v0.0.17 UID 模式**：identity.CredentialSpec（User/UID 两模式互斥）；config service/extension 增加 uid/gid/groups、run_as_uid/gid/groups；ValidateService/ValidateExtension 互斥校验；前端 4 处表单身份模式切换；规格 §2.2.13/§2.2.3 重写。
- **v0.0.18 CI 缓存修复**：Docker 构建缓存 gha（10GB 限制）→ `type=registry`（GHCR buildcache-<arch> tag）；Skill 同步 UID 模式（5 文件）；`.trae/` 用 `git add -f` 补充跟踪。

## 2026-07-24

- **Transmission 服务与扩展**：dev_workspace/transmission 服务 + tracker-updater 扩展开发。
- **端口探测 BUG（Yama LSM）**：ptrace_scope 限制降级方案；Docker 需 `cap_add: SYS_PTRACE`。
- **进程信息展示 UID/GID/用户组**。
- **待办梳理 + 三项优化**：DEV-007/011/012 经用户决策直接更新规格 v1.5（history 相对路径、triggers map 格式、actions 移除 icon/enabled）；① tracker-updater 改 session-set + default_trackers（RPC 动态读 rpc-port）；② 进程树 DFS 递归（buildPPIDMap + collectTreeDFS，resource_collector.go）；③ CPU 首次采样补偿（procEntry.baselineSet，200ms 重采样）。
- **M-04-001/TD-003 supervisor 重构**：新建 `internal/core/supervisor.go`（RunSupervisor/DecideRestart/handleProcessExit/doBackoff/doRestartProcess）+ supervisor_test.go；bootstrap/service_operator 的 superviseService 降为薄包装；圈复杂度 43→3、32→6；修复方案 3 处 ctx/error 硬伤 + RestartActionAbort（P1）。
- **v0.0.21**：重构审计（逐行比对无行为回归）+ 7 场景运行测试全通过。
- **v0.0.20/21 DEV-010**：状态机新增 `EventNormalExit`（规则 11：up/ready/starting → down），on-failure + exit 0 走合法转移替代 ResetTo；规格 §2.1.1 同步。
- **测试覆盖率提升 P0~P4 闭环**：L-01-001（api 43%→65%+，httptest hermetic 不改生产代码）/ L-04-001（前端主链路集成 + CI 接入）/ Go 核心包攻坚。
- **全面代码审计 98.37 分** + O-03-001 扩展日志结构化规范。
- （同日 audit/testplan 4 个过程性文档已随压缩清理，结论以本节为准。）

## 2026-07-25

- **E2E 验证 + v0.0.22**：资源收集/正常退出状态机/扩展日志注入/扩展回退（transmission-updater WASM 降级）4 项通过；测试脚本对齐 `status` 键名与 restart 嵌套 YAML 结构。
- **端口探测降级清理**：Docker `cap_add: SYS_PTRACE` 验证生效后，删除 UID/cmdline/ss/netstat 降级，仅保留受管 PID 进程树 fd socket inode 精确匹配（port_collector.go 等）。
- **auto-create-users 生命周期前移**：`post_ready` → `pre_start`（自启动服务前创建用户）；默认 enabled:true；docker-compose 加 ALLID。
- **v0.0.28**：审计通过（用户名正则过滤注入、幂等 id 判断）。

## 2026-07-26

- **v0.0.29**：auto-create-users find/UID-GID 迁移修复（`-a -prune -o` 显式化、非数字 ID 校验、BusyBox adduser -G、属主迁移失败传播、超时 300s）。
- **v0.0.30**：autostart:false 服务初始状态 pending → **down**（bootstrap.go 初始化时 ResetTo(StateDown)，规格 §2.8.1）。
- **v0.0.31**：Dockerfile 装 `shadow` 包（useradd/groupadd 等）；init 输出 ALLID/env 格式提示。
- **v0.0.32/33 GHCR latest 回退修复**：架构构建仅推 `${version}-${arch}`；正式版更新 latest 前拉全部 tags 仅允许最高语义版本；GitHub Latest Release 同理（禁止自动标记）。
- **前端**：ServiceDetail 增加版本 Badge 与 description 展示（i18n 补充）。

## 2026-07-27

- **8 阶段任务执行计划**：R-06 修复 meta.yaml 解析失败扩展消失（discovery 保留 Meta=nil + ConfigErrors 暴露至前端错误指示）；**R-06-REG-01 回归**：reload_classifier `classifyExtensionChange` typed nil panic——Go 类型断言对 typed nil 返回 ok=true，需显式 nil 检查；62/66 测试通过。
- **R-09 规格偏差**：规格补写 `ui.show_logs`/`ui.button_style`，移除无意义的 `ui.category`。
- **R-01/R-02**：资源采集 NSpid 精确映射 + 候选消歧 + 批量 CPU 采样降级；**R-03**：transmission-updater 改 curl 直写 .part + 原子 rename（tjs 无流式写文件）；**R-05**：按路径捕获 spawn 失败；**R-08**：buildStopConfigs nil 防御。

## 2026-07-28

- **v0.0.35**：R-01~R-09 审计 + 运行测试 13/13 通过；关键技术：NSpid、批量采样 200ms、curl 流式落盘。
- **v0.0.36 多 profile 导出**：`internal/config/package_profile.go`（LoadPackageProfile，名称正则防穿越）+ `PackDirWithProfile`（matchPattern 三语义）+ `GET /export-profiles` + 前端导出对话框；规格 §2.12.2 重写；Skill：bin/+data/ 目录规范、`tjs.open` FileHandle 真流式落盘（uv_fs_write）、downloadStream()、10-binary-updater-ext 示例。
- **Debian slim 镜像变体**：新建 `Dockerfile.debian`（bookworm-slim 三阶段，glibc 链接 tjs 不与 Alpine 互通）；release.yml/build-push.yml 增加 debian jobs（debian + vX.Y.Z-debian 标签）；手动 Alpine tjs 构建改 alpine:3.20 容器内编译 + 缓存 key 加 alpine 标识 + ld-musl 硬检查。
- **WASM 成品落地**：Skill assets/wasm 新增 zstd.wasm + bsdtar.wasm（固定来源 haskell-wasm/bsdtar-wasm，记录 SHA256 与许可证）；本地构建 txiki.js v26.6.0 验证往返一致；WASI 限制：bsdtar.wasm 不能创建归档（无 archive_read_disk）。
- **v0.0.37 审计修复 5 处**：F3/F5 **tjs `proc.wait()` 返回 `{exit_status, term_signal}` 非 `exitCode`**（误用 `exitCode ?? 0` 会把失败全判成功）；F2 tjs 无 Buffer 全局（用 TextDecoder）；F1 action 用 SUPD_ACTION 环境变量非 args[2]；F4 getArch async + /proc/cpuinfo。
- **扩展传参重构**：删除死代码 `Action.Args`（后端 12 文件 + 前端 + 规格）；新增 TempEnv 运行时参数（覆盖顺序：os.Environ → mergedEnv → TempEnv → supdEnv，可覆盖 env.yaml 不可覆盖 SUPD_*）；新建 Drawer.tsx + EnvParamsDrawer.tsx（非阻塞抽屉：保存/仅本次运行）。
- **v0.0.38**：CLI `ext run` 契约修正（action/env 非 action_id/env_overrides）；服务级未知 action 返回 400（原静默回退首个）；扩展列表补相对 env_path；Skill README 生成规范（8 必需标题 + validate_dev.py 校验）。
- **Startup Banner**：`api.Server.Start` 改 net.Listen + addrReady 回调（解决 :0 动态端口/IPv6 实际地址）；新建 startup_summary.go（两段式摘要、enumerateAccessURLs 枚举网卡）；auth_token 永不打印。

## 2026-07-29

- **v0.0.39**：post_ready 扩展改异步 `go`（HTTP goroutine 之后），不阻塞启动摘要；关机仍等待异步 post_ready（WaitForAllRunning）。dispatcher 成功运行不写 supd.log（仅扩展运行日志文件）。
- **规格一致性审计复核**：29 个 F 项逐项验证（F-7-001 1024px 守卫为误报）；产出 32 个整改方案（tmp/fix/，A~H 批次）。
- **审计整改 A~H 全量落地**：文件 API 分级白名单 + symlink 防逃逸（safeResolve 最近存在祖先解析）、原子写/历史契约、导入导出事务、生命周期串行化、启动信号与关机编排、runtime 错误传播、diagnostic 结构化字段白名单脱敏、serialize FIFO；**竞态修复**：HTTP Server Start/Stop 并发读写 httpServer 加 RWMutex（addrReady 须在发布 server 后调用）。
- **F2-001 serialize 队列满**：queue full 的 failed 记录缺 StartedAt（零值被 lazyCleanup 立即删除）→ dispatcher 统一补全元数据；serialize 上限 16 = pendingRuns（第 18 个触发 queue full）；RunExtension 是异步模式（预记录 TaskRunning 即返回 run_id）。

## 2026-07-30

- **clear-failed 修复**：`ClearFailedState` ResetTo(StatePending) → **StateDown**（无依赖服务卡 pending 假象；down 可 manual_start）；测试中发现额外 bug："not in failed state" 客户端错误 500 → 400（errors.As 识别 ServiceError）。规格 §2.8.1 允许 down/pending。
- **v0.0.40**：审计 + A~F 六组运行测试；Skill 同步 clear-failed/force-stop/signal 端点。

## 2026-07-31

- **Skill 全面验证**：pack_dev.py 修复 `re.fnmatch` 不存在 bug + PyYAML 回退解析器；validate_dev.py 增 4 项校验（version 三段正则/entry 路径安全/actions/restart/debounce/ui.button_style）+ tjs 执行权限误报修复（runtime 非空跳过）+ 多行 command 解析；7 个示例 entry `./run.sh` → `run.sh`；04_online_dev_guide 修正 runtimes install 描述。
- **热重载矩阵修正**（对照 reload_classifier.go）：depends_on 下次启动、command/user/uid 待重启、timeout_seconds 下次执行、signals 即时生效。

## 2026-08-02

- **Skill 配置结构完善**：统一目录契约（baseDir/服务/扩展/runtime）、entry/runtime 契约、env 来源区分、profile 规则；validate_dev.py 错误退出契约（FAIL 累计非零）+ 递归校验服务级扩展；pack_dev.py 默认 profile 三级回退；记录 Go ValidateService 未落实 version 正则的实现缺口。

## 2026-08-03

- **v0.0.42 多服务同名扩展查找竞态**：`GetExtension(name)` 按 map 迭代返回首个匹配（随机），7 处调用点受染（3 处静默数据损坏：Update/Delete/SaveEnv 可能写错服务）。新增 `GetExtensionForService(service, name)`（`Discovery.Services[svc].Extensions[name]` 精确索引，service="" 退化兼容导入预览）；5 个 provider 方法全部切换；api 包 +7 测试（含 80 请求确定性验证）。
- （同日提交 8-02 遗留的 dev_workspace/transmission 扩展：tracker-updater v1.3.0（3 源合并 + BEP-15 UDP 测速 + 49 优质 tier + 1 垫底 ≤50 组）、transmission-updater v1.1.0、pre-start-fixperms v1.1.0，run_as_uid 1000。）

## 2026-08-04

- **v0.0.43 workdir 相对路径**：`core.ResolveWorkdir` 统一解析；移除 workdir 绝对路径校验；6 处构建逻辑统一。
- **v0.0.44 全配置路径统一**：env_files/extension_dirs/runtimes/extension entry/CWD/command/readiness 全链路支持绝对+相对路径（`BuildRegistryAt(baseDir, ...)`）。
- **v0.0.45 runtime CLI 补漏**：`runtimes install` 接受相对路径（删除绝对路径限制）；install/remove PUT 契约改为直接 runtime map（移除错误包装层）。

## 2026-08-05

- **服务日志前缀时间 bug**：LogViewer 长轮询把所有行 timestamp 设为 `Date.now()`。新增 `parseLogContent()` 从 supd 日志行解析 RFC3339 时间戳（纳秒截断毫秒兜底）、[level]、message（去前缀）；解析失败回退旧行为。

## 2026-08-31

- **扩展 CWD/entry 解析根回归修复**（v0.0.44 引入，190 全部扩展启动失败）：`buildWorkDir` 从"扩展自身目录"误改为服务根/baseDir。恢复返回 `filepath.Dir(extEntry.ConfigPath)`；RunExtension workDir 改扩展目录（serviceDir 仅用于 SUPD_SERVICE_DIR）；导出/导入校验 entryRoot 同步；规格 §2.2.3/2.2.12/2.5.6/2.12.3 与 Skill 同步；SUPD_SCRIPT_TMP 变量代码从未注入（文档偏差，Skill 已修正为直接写绝对路径）。
- **v0.0.48 镜像层增量更新**：Dockerfile/Dockerfile.debian 拆分 tjs 包装脚本、supd、tjs 独立层 + `COPY --link --chmod=755`（Buildx 支持）。
- **v0.0.49 tjs 编译缓存增强**：缓存 key 加 schema/libc/架构/TJS_VERSION。
- **v0.0.50 tjs 固定 Release 缓存**：共用 `tjs-cache` prerelease（Actions Cache → Release asset → 容器内编译）；资产按 variant/arch/version/schema 区分；保留最近 5 版本；workflow 并发组防竞争。
- **v0.0.52**：build-push.yml 重复 job-level `if` 导致调度前解析失败——每个 job 合并为单一条件。
- **v0.0.53**：`gh release upload source#label` 只设显示 label 不改名 → 资产全叫 `tjs` 永不命中；改为先复制为完整目标名再上传 + 上传后校验 + 清理旧误名资产。
- **v0.0.54**：无代码变更的验证性发布，确认缓存链路（四资产 alpine/debian × amd64/arm64 v26.6.0-v3）。

## 2026-09-01

- **Skill 文档**：`SKILL.md` 底包 libc 门禁改为双向（Alpine↔Debian，musl 服务并非不能装于 Debian，补齐运行时即可用）；`01_service_spec.md` 新增 §1.8（musl 二进制运行于 Debian 的启用路径）；遗留项标注 alpine-init 不做底包感知改造（用户确认）。
- （当日 190 服务器上的服务排障、目录整理、smartdns 配置修复等远程运维操作未涉及 supd 源码变更，已按用户要求不记录于开发日志。）
