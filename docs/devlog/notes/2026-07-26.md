# 2026-07-26

## auto-create-users find/UID-GID 迁移修复与 v0.0.29

### 触发原因
审计 `auto-create-users` 扩展时验证用户提出的 `find -prune` 问题，并检查当前提交新增的 UID/GID 创建与迁移逻辑。

### 完成要点
- 实测并审计确认原 `find` 表达式的 `-prune -o` 语义正确，会跳过 `/proc`、`/sys`、`/dev`；改为显式 `-a -prune -o` 以提升可读性，不改变遍历语义。
- 修复 UID/GID 非数字输入校验、BusyBox `adduser -G` 组名参数、组准备失败传播。
- UID 变化与 GID 变化分别迁移文件属主和属组，检查 `timeout/find/chown/chgrp` 失败状态。
- 将扩展超时从 30 秒调整为 300 秒，失败结果改为 `error` 并返回非零状态。
- 按高权限内网/容器使用场景保留 `PW_FILE`/`GRP_FILE` 测试与运行时路径覆盖灵活性，不加入目标 UID/GID 冲突拦截等额外安全策略。
- 新增非数字 ID、迁移命令失败、显式 prune 表达式等测试覆盖。

### 修改文件
- `internal/cli/init_examples.go`
- `internal/cli/init_test.go`
- `README.md`
- `docs/devlog/session-notes.md`
- `docs/devlog/notes/2026-07-26.md`

### 验证
- `go test ./internal/cli -run AutoCreateUsers -count=1` 通过。
- `git diff --check` 通过。
- `go build ./...`、`go vet ./...`、`go test ./... -count=1` 全部通过。

### 版本
升级至 `v0.0.29`。

---

## autostart: false 服务初始状态修复

### 触发原因
用户反馈开机后首页左上角状态一直显示"过渡中"，原因是有配置为显式不启动（`autostart: false`）的服务。

### 根因分析
1. 在 `Bootstrap.Run` 阶段，对扫描发现的所有服务创建 `StateMachine`，初始状态默认均为 `StatePending` (`pending`)。
2. 在 `startAutostartServices` 阶段，筛选出 `autostart: false` 的服务跳过启动，但未将其状态重置为 `StateDown` (`down`)，导致其状态永久停留在 `pending`。
3. 前端 `StatusBar.tsx` 聚合健康度逻辑中，`hasTransition` 判断包含 `s.status === 'pending'`，因此只要有 `autostart: false` 的服务，顶部指示条就会持续判定为"过渡中" (`warning`)。

### 修复方案
- 在 `internal/core/bootstrap.go` 初始化 `StateMachine` 时，判断若服务 `autostart: false`（`!isAutostart(svcEntry.Config)`），则调用 `sm.ResetTo(StateDown)`，将其初始状态重置为 `down`（对齐需求规格 v1.5 §2.8.1 第 1474 行`autostart: false 的服务保持 down 状态`）。
- 更新 `internal/core/bootstrap_test.go` 中 `TestBootstrap_AutostartFalse` 单元测试断言，显式断言 `manual-svc` 的初始状态为 `StateDown`。

### 修改文件
- `internal/core/bootstrap.go`
- `internal/core/bootstrap_test.go`
- `docs/devlog/notes/2026-07-26.md`
- `docs/devlog/session-notes.md`

### 验证
- 后端：`go build ./...`、`go vet ./...`、`go test ./... -count=1`（全部通过）
- 前端：`cd web && pnpm build`（构建成功）

### 版本
升级至 `v0.0.30`。

---

## Docker 用户管理工具与启动配置提示优化

### 触发原因
根据 192.168.31.188:7979 上的 auto-create-users 扩展检查默认模板，补全 Docker 镜像用户管理命令，并在启动时提示 ALLID 与全局环境变量 YAML 的正确写法。

### 完成要点
- 对比确认 188 节点仍为旧版 v1.0.0；仓库默认 v1.1.0 已包含 UID/GID 校验、迁移失败传播、pre_start 触发及 300 秒超时，保留新版修复。
- Docker Alpine 运行时安装 `shadow`，提供 `useradd/groupadd/usermod/groupmod/userdel/groupdel`。
- auto-create-users 优先使用 `usermod/groupmod` 修正 UID/GID，保留 BusyBox 环境直接改写 passwd/group 的回退路径。
- supd 日志初始化后打印 ALLID 格式、示例和行为，以及 `env/00-base.yaml` 的结构化 YAML 示例；内容同时写入控制台与 `supd.log`。
- 初始化生成的 `env/00-base.yaml` 注释中加入可直接取消注释使用的 ALLID 示例。

### 修改文件
- `Dockerfile`
- `internal/cli/init.go`
- `internal/cli/init_examples.go`
- `internal/cli/init_test.go`
- `internal/core/bootstrap.go`
- `docs/devlog/notes/2026-07-26.md`
- `docs/devlog/session-notes.md`

### 验证
- `go build ./...`、`go vet ./...`、`go test ./... -count=1`、`git diff --check` 全部通过。
- 真实启动验证两条提示同时写入控制台和 `supd.log`。
- 当前环境无权访问 Docker daemon，未执行容器内命令存在性验证。


