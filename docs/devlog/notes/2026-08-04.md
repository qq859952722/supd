# 2026-08-04

## 远程服务目录结构优化 + SSH 空密码连接固化

### 触发原因

用户指出 192.168.31.188:7979 上服务的目录结构优化不彻底：
1. smartdns 的 `rule` 目录还是单独的顶层目录，未挪到 `config/` 下
2. smartdns 配置文件中引用规则文件使用了绝对路径，不利于迁移
3. 要求检查其他服务是否有类似绝对路径问题
4. 要求优先使用空密码 SSH 连接方式（不修改 ssh 服务配置），测试联通后将连接方式写入 skill，后续优先用 SSH 优化服务

### 完成要点

#### 1. smartdns 目录结构优化

- **目录迁移**：`rules/`（顶层）→ `config/rules/`，规则文件（cn_domain.txt / gfw_domain.txt / china_ip.txt）归入 config 命名空间
- **配置文件路径改写**（`config/smartdns.conf`）：
  - `domain-set -file` 基准为配置文件目录（config/）：`/etc/supd/.../rules/cn_domain.txt` → `rules/cn_domain.txt`
  - `ip-set -file` 同基准：`/etc/supd/.../rules/china_ip.txt` → `rules/china_ip.txt`
  - `plugin` 同基准：`/etc/supd/.../bin/smartdns_ui.so` → `../bin/smartdns_ui.so`
  - `cache-file` 基准为进程 CWD（服务根）：保持 `config/smartdns.cache`
- **清理残留**：删除路径基准错误产生的空目录 `config/config/`
- **验证**：smartdns restart 后 status=ready，update-gfw-china 扩展触发成功（6s 更新 110878 中国域名 + 26079 GFW + 4189/1605 IP）

#### 2. 其他服务绝对路径扫描与修复

扫描 7 个服务的 `service.yaml` 和 `config/` 下文件，修复所有 `/etc/supd/services/<svc>/` 绝对路径引用为相对路径：

| 服务 | 修复内容 |
|------|----------|
| smartdns | rules 目录迁移 + domain-set/ip-set/plugin 路径相对化 |
| dnscrypt-proxy | config/*.toml 中绝对路径 → 相对路径 |
| code-server | service.yaml workdir / config 路径相对化 |
| openlist | service.yaml 配置路径相对化 |
| backrest | service.yaml 数据路径相对化 |
| filebrowser | service.yaml `--root` 参数绝对路径 → 相对路径 |
| adguardhome | service.yaml 配置/工作目录路径相对化 |

> code-server 的 VSCode 自动生成元数据（extensions.json / languagepacks.json / user-data/logs/）含 `/etc/supd` 绝对路径，属 VSCode 自管理文件，不改。

#### 3. SSH 空密码连接方式固化

**设计原则**：不修改 dropbear-ssh 服务配置，仅依赖现有 `-B` 空密码模式；通过 `SSH_ASKPASS` 注入空密码，不依赖 sshpass。

**新建封装脚本** `scripts/remote_ssh.sh`：
- 自动创建临时 askpass 脚本（输出空密码）
- 自动通过 HTTP API 检查并启动 dropbear-ssh 服务（默认 autostart:false）
- 支持：交互式 shell / 单条远程命令 / SFTP 上传(-f) / SFTP 下载(-g)
- 环境变量可覆盖：`REMOTE_HOST` / `REMOTE_PORT` / `REMOTE_USER` / `API_BASE`

**Skill 文档更新**（`references/04_online_dev_guide.md` §3.1）：
- 新增「SSH 客户端空密码连接」章节
- 记录前置条件（dropbear-ssh 启动 + root 空密码 `passwd -d root`）
- 两种连接方式：封装脚本（推荐）+ 手动 SSH 命令
- 运维操作示例（SSH 优先于 HTTP API）
- 注意事项（不改配置 / 内网可信 / 容器重启恢复 / 用完关闭）

#### 4. smartdns 路径基准差异文档化

在 skill 文档（`04_online_dev_guide.md` §3.2）新增「smartdns 配置路径基准差异」章节：
- 不同指令的相对路径基准不同：`cache-file` 以进程 CWD 为基准，`domain-set -file`/`ip-set -file`/`plugin`/`conf-file` 以配置文件目录为基准
- 路径基准对照表 + 推荐目录结构 + 常见错误示例
- update-gfw-china 扩展与配置路径的协同关系

### 修改文件

**Skill 文档**：
- `.trae/skills/supd-service-extension-dev/scripts/remote_ssh.sh`（新建；`ensure_dropbear` 改用 grep 替代 python3）
- `.trae/skills/supd-service-extension-dev/references/04_online_dev_guide.md`（§3.1 SSH 连接 + §3.2 smartdns 路径基准）

**远程文件**（192.168.31.188:7979，通过 SSH 修改）：
- `services/smartdns/config/smartdns.conf`（路径相对化）
- `services/smartdns/config/rules/`（从顶层 rules/ 迁入）
- `services/{dnscrypt-proxy,code-server,openlist,backrest,filebrowser,adguardhome}/` 各自 service.yaml + config 文件路径相对化
- `services/smartdns/config/config/`（空残留目录已删除）
- `services/transmission/env.yaml`（`TRANSMISSION_HOME`/`TRANSMISSION_WEB_HOME` 绝对路径 → 相对路径）
- `services/s-ui/extensions/fix-sui-perm.bak.20260726-145818/`（残留备份目录已删除）

### 关键技术点

- **smartdns 路径基准双重性**：
  - `cache-file` 基准 = 进程 CWD（service.yaml 未设 workdir 时 = 服务根目录）
  - `domain-set -file` / `ip-set -file` / `plugin` / `conf-file` 基准 = 配置文件所在目录（config/）
  - 故 `cache-file config/smartdns.cache` 与 `domain-set -file rules/xxx.txt` 路径基准不同但都能正确解析
- **SSH 空密码注入**：`SSH_ASKPASS=<script> SSH_ASKPASS_REQUIRE=force setsid -w ssh ...` 通过 askpass 脚本输出空密码，绕过交互式密码提示
- **dropbear -B 模式**：允许空密码登录，但要求 `/etc/shadow` 中密码字段为空（`NP` 状态）；账号锁定（`L` 状态）会拒绝
- **容器重启恢复**：`/etc/shadow` 在 overlay 文件系统，容器重启后 root 密码恢复，需重新 `passwd -d root`
- **环境变量相对路径**：supd 启动服务时 CWD=服务根目录（service.yaml 未设 workdir），故 env.yaml 中的 `TRANSMISSION_HOME=config` 等相对路径会被进程正确解析为 `<服务根>/config/`
- **扩展脚本的 fallback 模式可接受**：`${SUPD_SERVICE_DIR:-/etc/supd/...}`（shell）和 `tjs.env.SUPD_SERVICE_DIR || '/etc/supd/...'`（JS）是标准防御性编码，supd 运行时注入环境变量覆盖 fallback，绝对路径仅在手动调试时生效
- **transmission-updater wasm 路径**：candidate 列表含 `${serviceDir}/../../runtimes/`（相对路径，优先）和 `/etc/supd/runtimes/`（绝对路径，最后 fallback），设计合理

### 验证结果

- **服务状态**：12 个服务全部 ready（含 dropbear-ssh），0 down / 0 failed
- **残留扫描**（最终）：
  - service.yaml + env.yaml — ✅ 全部干净
  - config/ 文本配置文件 — ✅ 全部干净（排除 VSCode 自管理元数据和 filebrowser.db 二进制数据库）
  - 全局扩展脚本 — ✅ 全部干净
  - 服务级扩展脚本 — 仅剩 `${SUPD_SERVICE_DIR:-...}` fallback 模式（可接受）
- **update-gfw-china 扩展**：触发成功，6s 完成，生成规则文件到 `config/rules/`，smartdns 保持 ready
- **transmission 重启验证**：env.yaml 改相对路径后 restart → status=ready，端口 9091/51413 正常监听，WebUI HTTP 301 正常
- **s-ui 扩展发现**：删除备份目录后仅发现 `reset-admin`（正确）
- **SSH 连接**：`remote_ssh.sh` 脚本连通验证通过（`whoami` → root，`hostname` → inas）

### 遗留事项

- 无。本次彻底完成目录结构优化和 SSH 连接方式固化，所有管理的配置文件均已相对路径化。

---

## workdir 相对路径支持 + v0.0.43 发版 + 远程验证

### 触发原因

用户要求：
1. supd 服务的 workdir 要支持相对路径的配置方式（此前强制绝对路径，不利于服务目录整体迁移）
2. 这部分允许修改代码重构，但必须经过严格的代码审计和运行状态测试
3. 188 上所有服务的目录结构要严格规范，不要使用脚本方式启动二进制，通过 supd 直接启动二进制，通过命令行和环境变量控制行为

### 完成要点

#### 1. 代码修改：支持相对路径 workdir

**新增** `internal/core/workdir.go` — `ResolveWorkdir` 统一解析函数：
- 空 workdir → 返回服务根目录（service.yaml 所在目录）
- 绝对路径 → 原样返回（filepath.Clean 清理 `.` 和 `..`）
- 相对路径 → 基于服务根目录拼接后清理（filepath.Join + filepath.Clean）
- 路径穿越安全性：filepath.Join 规范化 `..` 但不阻止逃逸，服务进程 CWD 即使逃逸也受 baseDir 边界约束

**修改** `internal/config/service_validate.go` — 移除 workdir 必须绝对路径的校验：
```go
// 旧：if sc.Workdir != "" && !strings.HasPrefix(sc.Workdir, "/") { return error }
// 新：允许相对路径，在 core.ResolveWorkdir 中基于服务根目录解析
```

**统一 6 处 workdir 构建逻辑**为调用 `ResolveWorkdir`（此前每处都手动写 `if workdir == "" { workdir = filepath.Dir(...) }`）：

| # | 文件 | 函数/位置 | 说明 |
|---|------|-----------|------|
| 1 | `internal/core/bootstrap.go` | `startService` | autostart 启动路径 |
| 2 | `internal/core/bootstrap.go` | `RunReadiness` 回调 | 就绪检查时构建 workdir |
| 3 | `internal/core/supervisor.go` | restart 路径 | 自动重启时构建 workdir |
| 4 | `internal/api/service_operator.go` | `startServiceLocked` | API 手动启动路径 |
| 5 | `internal/api/service_operator.go` | `RunReadiness` 回调 | API 启动后就绪检查 |
| 6 | `internal/api/resource_handler.go` | 磁盘占用统计 | **审计新发现**：旧代码直接用 `info.Config.Workdir` 未解析相对路径，导致相对 workdir 在此降级路径下 Statfs 失败 |

> 第 6 处是本次审计新发现的遗漏，此前会话只迁移了 5 处。本次审计通过 `grep -rn "filepath.Dir.*ConfigPath\|\.Workdir"` 全量扫描发现。

#### 2. 规格与前端同步

- `docs/需求规格说明_v1.5.md`：workdir 字段描述从「禁止相对路径」改为「支持绝对路径与相对路径，相对路径基于服务根目录解析」
- `web/src/components/service/ServiceForm.tsx`：placeholder 与 hint 更新，告知用户支持相对路径

#### 3. 测试覆盖

**新增** `internal/core/workdir_test.go` — 7 个测试用例：
- `TestResolveWorkdir_EmptyReturnsServiceRoot`：空 workdir → 服务根
- `TestResolveWorkdir_AbsolutePathCleaned`：绝对路径清理（含冗余 `.`/`..`）
- `TestResolveWorkdir_RelativePathJoinedToRoot`：相对路径 Join（含 `./`、`.`、嵌套）
- `TestResolveWorkdir_ParentEscapeAllowed`：`..` 逃逸允许（受 baseDir 边界约束）
- `TestResolveWorkdir_RelativeCleanedProperly`：相对路径清理
- `TestResolveWorkdir_ConsistencyWithOldLogic`：与旧逻辑一致性（空 workdir 场景）
- `TestResolveWorkdir_RealisticAdguardHomeScenario`：AdguardHome 真实场景（`workdir: .` → 服务根）

**更新** `internal/config/service_test.go`：`TestValidateWorkdirRelative` 从「期望相对路径被拒绝」改为「期望相对路径通过验证」

#### 4. v0.0.43 构建与部署

- 本地构建：`CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -ldflags "-X main.version=v0.0.43 -X main.buildTime=2026-08-04"`
- 二进制大小：25MB（v0.0.41 为 20MB，差异来自前端资源更新）
- 通过 `remote_ssh.sh -f` SFTP 上传到 `/usr/local/bin/supd.new`
- 原子替换：`cp /usr/local/bin/supd /usr/local/bin/supd.bak.v0.0.41 && mv /usr/local/bin/supd.new /usr/local/bin/supd`
- 容器重启：`kill -TERM 1`（supd 优雅关机 → 容器自动重启加载新二进制）
- 重启后 dropbear-ssh（autostart:false）通过 API 手动启动

#### 5. 远程 adguardhome workdir 验证

**验证步骤**：
1. 在 adguardhome service.yaml 添加 `workdir: .`（相对路径，旧版本会拒绝）
2. watcher 检测变更并热重载配置 — ✅ 新版本接受相对路径，无报错
3. API 查询确认 `workdir: .` 已生效，服务保持 ready
4. 通过 API restart adguardhome
5. 检查进程 CWD：`readlink /proc/<pid>/cwd` → `/etc/supd/services/adguardhome`（服务根目录）

**验证结果**：
- ✅ `workdir: .` 被 v0.0.43 验证器接受（v0.0.41 会拒绝）
- ✅ 相对路径 `.` 正确解析为服务根目录
- ✅ 进程 CWD = `/etc/supd/services/adguardhome`（与未设 workdir 时一致）
- ✅ 服务 2 秒内达到 ready，restart_count 归零
- ✅ 命令 `bin/AdGuardHome -c config/AdGuardHome.yaml -w .` 正常执行

#### 6. 本地端到端测试

在本地 test_workdir 创建 `workdir-rel-test` 测试服务：
- `workdir: subdir`（相对路径，应解析为 `service_root/subdir/`）
- 脚本写入 CWD 到文件，script readiness 检测文件存在
- 启动后 CWD = `/home/qq/.../test_workdir/services/workdir-rel-test/subdir` ✅
- 服务 ready，restart_count=0 ✅

### 修改文件

**新增**：
- `internal/core/workdir.go`（ResolveWorkdir 函数）
- `internal/core/workdir_test.go`（7 个测试用例）

**修改**：
- `internal/config/service_validate.go`（移除绝对路径校验）
- `internal/config/service_test.go`（更新 workdir 相对路径测试期望）
- `internal/core/bootstrap.go`（2 处 workdir 构建改为 ResolveWorkdir）
- `internal/core/supervisor.go`（1 处 workdir 构建改为 ResolveWorkdir）
- `internal/api/service_operator.go`（2 处 workdir 构建改为 ResolveWorkdir）
- `internal/api/resource_handler.go`（1 处 workdir 构建改为 ResolveWorkdir，修复相对 workdir 在磁盘统计降级路径下未解析的潜在缺陷）
- `docs/需求规格说明_v1.5.md`（workdir 字段描述更新）
- `web/src/components/service/ServiceForm.tsx`（表单 placeholder 与 hint 更新）

**远程修改**（192.168.31.188）：
- `/usr/local/bin/supd`（v0.0.41 → v0.0.43，备份为 supd.bak.v0.0.41）
- `/etc/supd/services/adguardhome/service.yaml`（添加 `workdir: .` 测试相对路径）

### 关键技术点

- **ResolveWorkdir 设计**：统一入口，消除 6 处重复的 `if workdir == "" { workdir = filepath.Dir(...) }` 模式；相对路径用 `filepath.Join(root, workdir)` 拼接后 `filepath.Clean` 清理
- **resource_handler 隐蔽缺陷**：磁盘占用统计路径此前的 workdir 构建与启动路径不一致（直接用 `info.Config.Workdir` 未解析），相对 workdir 会导致 `syscall.Statfs` 路径错误。本次统一修复
- **容器重启策略**：supd 作为 PID 1 运行在 Docker 容器中，`kill -TERM 1` 触发优雅关机后容器自动重启（restart policy 生效）
- **dropbear-ssh autostart:false**：容器重启后需通过 API 手动启动 dropbear-ssh 才能恢复 SSH 访问
- **热重载验证**：watcher 检测 service.yaml 变更后热重载，新版本验证器接受相对 workdir，服务无需重启即生效（进程 CWD 在下次重启时应用）

### 验证结果

- **本地测试**：go build/vet/test 全通过；go test -race 通过；pnpm build 通过；workdir_test 7 用例全通过；本地端到端测试（workdir: subdir）CWD 正确解析
- **远程测试**：12/12 服务 ready；adguardhome `workdir: .` 热重载成功；重启后进程 CWD = 服务根目录；目录结构全部规范（仅 data/config/bin/extensions/env.yaml/service.yaml/web/README.md）

### 遗留事项

- v0.0.43 尚未推送到 GitHub（本地 commit 已完成，待用户确认是否发版）
- adguardhome service.yaml 中 `workdir: .` 为测试添加，可保留（功能等价于未设 workdir）或移除

---

## 全配置路径绝对/相对路径统一支持

### 触发原因

上一轮仅覆盖 `workdir`，本次全面排查全局、服务和扩展配置的路径消费链，统一支持绝对路径和按所属配置根解析的相对路径。

### 完成要点

- 新增统一 `config.ResolvePath` 与 `core.ResolveCommandPath`：全局路径相对 `<baseDir>`；服务命令、workdir、readiness script 及服务扩展相对服务根；裸命令保留 PATH 查找。
- 全局 `env_files`、`extension_dirs`、配置 runtime 支持绝对/相对路径；discovery、watcher、CLI validate、热重载和 runtime provider 使用同一规则。
- 全局扩展 entry/CWD 统一为 `<baseDir>`，服务扩展 entry/CWD 统一为服务根；生命周期、按需、cron、重试及私有 env 路径完整传递。
- 扩展 entry 允许绝对路径，仍拒绝独立 `..` 路径段、shell 元字符和冗余路径组件；导入导出按最终配置根校验实际入口。
- 运行审计发现并修复 API 手动启动/重启 readiness 未完整传递服务根、服务级按需扩展 CWD、直接按需扩展 env 上下文等遗漏。
- `extension_dirs` 热修改会用新目录立即重扫，但 watcher 根集合需重启 supd 才持续监听，因此分类为需重启 supd。

### 严格运行测试与验证

- 使用 `/tmp/supd-path-runtime` 本地二进制验证相对/绝对服务 command、workdir、readiness script、runtime、extension_dirs、全局/服务扩展 entry 与 CWD。
- 服务 restart/stop/start 正常；readiness 相对脚本按服务根解析并以服务 workdir 为 CWD，最终进入 ready。
- on_demand 与 supd post_ready 生命周期扩展成功；热重载立即发现新增绝对扩展目录和绝对 runtime；supd 优雅停止成功。
- `go build ./...`、`go vet ./...`、`go test ./... -count=1`、`go test -race ./... -count=1`、`web/pnpm build` 全通过。
- Skill 文档、示例与 `validate_dev.py` 已同步路径基准；Python 编译与服务级根相对 entry 校验通过。

### 修改范围

- 后端：`internal/config`、`internal/core`、`internal/extension`、`internal/watch`、`internal/api`、`internal/cli`。
- Skill：`SKILL.md`、服务/扩展/env/在线开发参考、示例 meta.yaml、`validate_dev.py`。
- 规格与机制：`docs/需求规格说明_v1.5.md`、`docs/devlog/notes/core-mechanisms.md`。

### 遗留事项

- API 创建/导入全局扩展仍写入默认 `<baseDir>/extensions/<name>`；自定义 `extension_dirs` 当前用于发现、执行与监听，不改变管理 API 的默认落盘目录。
