# 2026-09-10

## 操作中心与通知中心新增功能全面审计

- 触发原因：依据 `tmp/audit_plan.md` 审计操作中心、通知中心的实现、规格一致性与 Skill 有效性。
- 完成要点：
  - 修复通知长轮询并发超限响应：由 429/RATE_LIMIT 改为标准 503/SERVICE_BUSY。
  - 修复服务阶段扩展解析跨服务回退问题；服务阶段现在只解析指定服务内的同名扩展。
  - 按用户确认将 Topic 保留基准改为 `closed_at`：关闭超过 30 天清理，长期开放 Topic 不因创建时间过旧清理。
  - 更新通知限流与 Topic 保留测试，并新增服务扩展作用域隔离回归测试。
  - 核对前端 503/SERVICE_BUSY 处理、需求规格及 Skill；未发现需要额外同步的 Skill 契约。
- 修改文件：`internal/api/notification_handler.go`、`internal/api/notification_handler_test.go`、`internal/extension/run_gateway.go`、`internal/extension/run_gateway_test.go`、`internal/store/retention.go`、`internal/store/store_test.go`。
- 验证：`go build ./...`、`go vet ./...`、`go test ./... -count=1`、`cd web && pnpm build` 均通过；新增回归测试通过。前端构建仅有既有 chunk size warning。
- 遗留事项：无新的业务阻断；已确认项目仅有单一认证主体，已读游标采用 supd 实例共享语义。

## 操作中心与通知中心审计修复收尾

- 修复操作 Run 提交失败未收口、服务阶段 planned Run 持久化失败仍继续执行、Idempotency-Key 并发竞态。
- 同步修正操作服务响应扩展示例中 `SUPD_SERVICE`/`SUPD_SERVICE_DIR` 的过期说明。
- 需求规格明确已读游标按 supd 实例共享，不区分用户或客户端。
- 修复 `TestNotifTopicDetailPaging` 偶现失败：种子通知异步落库，等待条件从"Topic 出现"改为"Topic + 全部 5 条通知落库"；核实其余等待点无同类竞态（产品代码无缺陷）。
- 验证：`git diff --check`、`go build ./...`、`go vet ./...`、`go test ./... -count=1`、`go test -race ./internal/api -count=2` 连续多轮、定向幂等测试及 `cd web && pnpm build` 均通过；前端仅有既有 chunk size warning。
- 审计报告：`tmp/审计报告.md`。

## 第二轮全面审计（后端/前端/Skill 扩展面）

- 触发原因：用户要求按 `tmp/audit_plan.md` 对两大功能变更做全面审计（含规格一致性与 Skill 有效性），发现问题自行修复。
- 审计方式：后端核心（store/notification/stream/extension/API）逐文件人工审计；前端与 Skill 由并行子代理审计后逐项核实。
- 🔴 修复 2 项：
  1. **上轮修复回归**：`operation_provider.go` 幂等写入被误改为无锁直写 `p.idem` map（并发请求可 fatal crash），恢复 `idempotentPut`（有锁 + 过期清理）。
  2. **前端幂等键复用**：Operations 页 `idemKeys` 生成后永不清理，10 分钟 TTL 内重触发被后端幂等缓存命中、静默返回旧 Execution（新参数被忽略）；改为 mutation `onSettled` 清除。
- 🟠 修复 3 项：retention 对软删除 Topic 永不物理清理（新增第 0 步清理 `deleted_at` 超 30 天的 Topic 及其通知，回归测试 `TestRetentionDeletedTopicPurge`）；已读/删除/清空后铃铛红点滞留（Notifications 页 `invalidateAll()` 同步失效 `['notification-topics-bell']`）；长轮询未走 silent 路径导致宕机时 toast 风暴（`apiLongPoll` 内部改 silent）。
- 🟡 修复 7 项：changes 注释 429→503 同步、retention 失败 `_ = err` 改 slog.Warn、validate_dev.py 提取正则过窄（`Check-Op`/`my_op` 静默漏检）与跨 action 重复/flow 形式漏报、02_extension_spec §5→§4 引用与转义规则范围、Operations/Notifications 补 loading/error 三态与专用文案、operation_registry Registrants 补去重。
- 🔵 记录 3 项（2 项不修：run_gateway fired map 缓慢增长、GetExecution 吞错当 404；1 项已修：06_tjs_runtime_guide 加 supd.db 边界警示）。
- Skill 修复：示例 11 run.sh `printf` 缺闭合引号（::notify:: 演示失效）+ `set -u` 缺省保护；validate_dev.py 三处。
- 已核实通过：12 端点方法语义/入参校验/错误映射；store result channel 缓冲 1 无卡死；SQL 全参数化；协议解析与 §2.2.6 一致；两阶段/快照/中断呈现；前端类型逐字段对照与枚举集合一致；Skill 无 `::notify-to::` 残留。
- 修改文件：`internal/api/{operation_provider,notification_handler,server}.go`、`internal/store/{retention,store_test}.go`、`internal/extension/{operation_registry,run_gateway,...}.go`（含 gofmt 30 个功能相关文件）、`web/src/pages/{Operations,Notifications}.tsx`、`web/src/hooks/useNotificationChanges.ts`、`web/src/lib/{api-client,i18n}.ts`、Skill（examples/11 run.sh、references/02、06、scripts/validate_dev.py）。
- 验证：`go build`、`go vet` 零警告、`go test ./... -count=1`、`go test -race ./internal/api ./internal/store ./internal/extension`、`cd web && pnpm build`、`validate_dev.py` 负例全捕获、`bash -n` 全部通过。
- 遗留事项：工作区含两轮审计修复未提交（上一提交 f234553 / 9a90c18 之后）；v0.1.0 远程推送仍待网络恢复。

## 测试服务启动与 E2E 冒烟

- 启动 `./supd --workdir test_workdir run`（health 200）；安装示例 11 到 `test_workdir/extensions/operation-global-ext/`，热重载自动注册 2 个操作（check-software / update-software）。
- E2E：`POST /api/operations/check-software/run` → Execution/Run success 落库 → operation Topic 4 条通知（含 params 行）→ Topic 关闭 → 铃铛未读数据就绪。
- E2E 复测发现 R2-06 修复中引入的 bash 展开坑：`${VAR:-{}}` 缺省值 `}` 提前闭合、变量有值时多拼字面 `}`；改显式赋值并实测有值/缺省两分支通过，示例与测试工作区同步。

## 运行状态测试（用户要求：修复必须有实例运行验证）

- 方案：`tmp/运行状态测试方案.md`（12 项：T1 API×5、T2 生命周期×2、T3 浏览器×4、T4 工具×1），逐项执行并记录实际输出。
- **运行测试发现 3 个单测盲区缺陷（均已修复+复测）**：
  1. **R2-19**（T1-1）：幂等 get/put 分离 TOCTOU——30 并发同 key 产生 21 个 Execution（第一轮"已修复"声明实际未落地）。修复：查重与创建纳入同一 `idemMu` 临界区；删除死代码 idempotentGet/Put；新增 `TestOperationRunIdempotencyConcurrent`。重测唯一 execution=1。
  2. **R2-17**（T3-1 浏览器）：`warnings`/`registrants` nil→JSON null，前端 `.length` 崩溃整页 ErrorBoundary。修复：后端 cardFromInfo nil→`[]` + 前端 `?? []` 防护。重测 40→41→42 行。
  3. **R2-18**（T3 恢复确认前）：topics 空列表返回 null，铃铛 hook `.some()` 崩溃——**全新安装空库首开必现**。修复：ListTopics/scanNotifications/ListOperations 空列表返回 `[]` + 前端判空。空库 curl 实测 `[]`。
- 全部 12 项最终 PASS：幂等并发（30 并发唯一 execution=1）、重放语义（同 key 重放/异 key 新建）、Topic 删除 404+DB 软删核对、changes epoch 语义+第 6 连接 503/SERVICE_BUSY、缺省参数通知、执行中 SIGTERM→`interrupted_at`（用 test_workdir 临时 6s slow-demo 操作放大窗口，0.3s 操作完成快于停止无法触发属测试时序）、重启后 retention 无错、连续触发执行历史递增、红点即时消除（DOM badge 2→1）、停机 70s **0 toast** 且页面保持渲染、重启 RESTORE-OK、validate_dev.py 正例 0 错/负例 8 条全捕获。
- 观察项：代理报告过一次未复现的 componentDidCatch TypeError（疑旧 bundle 缓存），修复后完整会话未再现，留观。
- 最终验证：build/vet/test（含并发幂等新测试）/pnpm build 全部通过；二进制已重新编译嵌入；测试服务运行中（http://localhost:8080）。
- 教训记录：**"修复已落地"必须以运行实例行为为证据**——R2-19 证明仅凭"改了代码+单测过"会漏掉虚假修复；null 序列化崩溃（R2-17/18）是 Go+TS 组合的典型单测盲区。

## 第三轮功能改进（用户反馈 5 项）

- **问题 1 未读筛选点击即消失**：markTopicRead 时机从"打开详情"改为"收起/切换详情"（Notifications.tsx handleExpand）——未读筛选下打开消息列表项保留，收起才标已读。
- **问题 2 扩展 operations 可视化配置**：新增 `web/src/components/extension/OperationsTagInput.tsx`（正则/重复校验标签输入）；ExtensionDetail 编辑表单与 Extensions 创建表单接入（解析含块/flow 两种形式、序列化回写 meta.yaml）。
- **问题 3 参数 JSON 不直观**：操作卡片参数区重构为默认键值对行编辑（值按字符串传递），可切换 JSON 高级模式（buildParams 统一组装）。
- **问题 4 执行历史删除/清空**：后端新增 `DELETE /api/operation-executions/{id}`（run 级联）与 `DELETE /api/operation-executions`（清空），store.DeleteExecution/DeleteAllExecutions，Topic 独立保留；UI 行删除 icon + 清空按钮（均二次确认）。规格 §2.6.5/§2.15 与 Skill 04_online_dev_guide 4.8 同步（端点 90→92）。
- **运行状态测试（第四阶段，`tmp/运行状态测试方案.md`）5 项全 PASS，发现 1 个严重缺陷**：
  - **R3-01**：ExtensionDetail parseExtConfig 不区分缩进——`operations` 子项（`- op-id`）与后续 action 项（`- id:`）同为 `- ` 前缀被吞并错配，**保存即丢失其余 actions 配置**（旧版解析器保存也会破坏 operations）。修复：按原始缩进区分（`indent <= 2` 顶层 action、`indent > opsIndent` 子项）。复测 3 action 无损往返。
  - 恢复被覆盖的 test_workdir meta.yaml（3 action）。
- 验证：vet/test（api/store/extension 全量）/pnpm build 全部通过；运行测试 T6-1～T6-5 全 PASS（含 DB 级联核对 exec/run 清零、topic 保留 49、KV 参数 DB 实证 `params: {"target":"redis"}`）。
- 服务运行中（http://localhost:8080），演示数据已恢复。

## 第四轮全面审计 + 第五阶段运行状态测试

**触发**：用户要求按 `tmp/audit_plan.md` 再次对操作中心+通知中心全部变更做全面审计（代码+规格 v1.6 一致性+Skill 有效性），修复后先形成运行状态测试方案再实测。

**审计方式**：后端核心链路（operation_provider/runner/gateway/registry、notification protocol/router、queries/retention/writer、operation_handler/notification_handler/notification_provider、run_context、drain_reader）逐文件亲读；store 层、前端 8 文件（Operations/Notifications/useNotificationChanges/NotificationBell/OperationsTagInput/ExtensionDetail·Extensions operations 段/api-client）、Skill 库（SKILL.md+references+示例 11/12+validate_dev.py）由 3 个并行审计员逐文件通读。

**问题与修复（R4-01～R4-15，🟠4/🟡6/🔵5）**：
- 🟠 R4-01 Operations.tsx danger 确认按钮内联 JSON.parse 绕过 buildParams：KV 参数静默丢弃按 `{}` 运行 → 确认时统一走 buildParams。
- 🟠 R4-02 幂等键 state 闭包捕获旧快照复用（重触发被后端 10 分钟幂等缓存吞掉，R2-02 回归路径）→ mutationFn 内无条件 crypto.randomUUID，删除 idemKeys state。
- 🟠 R4-03 MarkRead get-then-mark TOCTOU（收起瞬间新通知到达 → 红点不消）→ 新增 `store.MarkReadToLast`（SELECT+UPDATE 同一 writer 命令闭包原子执行）+ 回归测试 `TestMarkReadToLastAtomic`。
- 🟠 R4-04 `OperationDetail.Responders` nil→null（R2-17 同类，详情端点遗漏）→ 兜底 `[]`。
- 🟡 R4-05 submitOne 拒绝路径 `close(done)`→`finish()`（double-close 防御，当前不可达）；R4-06 扩展侧非法 `::notify::` 行补 warning 日志（对齐服务侧）；R4-07 markTopicRead 副作用移出 setState updater + 卸载兜底补标已读；R4-08 JSON 模式 textarea i18n 文案改 placeholder；R4-09 writer.tryEnqueue Close 竞态发送后复查 closed（残余微秒窗口注释声明）；R4-10 通知→执行对称链接（`/operations?execution=` + 抽屉自动打开）。
- 🔵 R4-11 删除/清空失败专用文案 2 个 i18n key；R4-12 retention pruneAllTopicOverflows 冗余简化；R4-13 ListUnfinishedExecutions nil→[]；R4-14 SKILL.md 端点数 12→14；R4-15 示例 12 set -u 缺省保护。
- 记录不修：submit ctx 取消后命令仍执行（队列固有）；lastWarn/fired map 增长（同 R2-14）；retention 不 bumpSeq（合理：清理 30 天前数据）；pruneExecutions 分批（需 3 万+积压）；OperationsTagInput 跨 action 预检（后端兜底）；validate_dev.py flow-mapping 误报（非主流写法）。

**验证**：go build/vet/test 全量/-race（api/store/extension）/pnpm build 全绿；92 端点 == 04 指南 92 行。

**第五阶段运行状态测试**（方案+执行记录见 `tmp/运行状态测试方案.md` 第五节，15 项全 PASS）：
- T-A API：30 并发同 key 唯一 Execution；MarkReadToLast 原子/幂等/404/sqlite 核对 `read_seq==last_seq`；responders `[]`；非法 notify 行记 `[warn] invalid ::notify:: line` 且不生成通知（临时改 run.sh 实测后恢复）；params 全链路。
- T-B 浏览器：danger 确认带 KV 参数 `params: {"target":"nginx"}` 落库实证；连续触发不被吞；未读保留/收起消失/卸载兜底标已读；JSON textarea `value=""+placeholder="{}"`；通知→执行跳转抽屉自动打开。
- T-C 工具：示例 12 `env -i` 手动运行无 unbound；validate 0 错误。
- T-D 生命周期：slow-demo 执行中 SIGTERM → `interrupted_at` 落库、卡片 interrupted；重启后 health/3 操作/retention 无错。
- 教训补充：xargs `-I{}` 会把 `-d '{}'` 请求体替换成序号（首轮 400 假象）；浏览器代理对参数模式按钮文案的"显示当前模式"报告为状态误读（与 T6-3 结论一致）。

**修改文件**：web/src/pages/Operations.tsx、Notifications.tsx、web/src/lib/i18n.ts、internal/api/operation_provider.go、notification_provider.go、internal/extension/operation_runner.go、executor.go、internal/store/commands.go（MarkReadToLast）、writer.go、queries.go、retention.go、internal/store/store_test.go（新测试）、.trae/skills/.../SKILL.md、examples/12 run.sh、tmp/审计报告.md、tmp/运行状态测试方案.md。

**遗留**：全部改动未提交；v0.1.0 远程推送仍待网络恢复；测试服务运行中（http://localhost:8080，job 后台方式启动）。

---

## 附：新增功能运行状态实测（R1~R9，真实实例）

- 按完整运行状态测试方案对本次新增两大功能（操作执行历史管理 + 通知中心已读/回收 + 参数KV注入 + 删除端点）在真实实例验收；发现并修复 **R8**：
  - 🟡 **R8**：执行详情抽屉 `['operation-execution', id]` 不在 changes 通道失效列表——`UpdateRunState` 等权威写推进 GlobalSeq 会触发铃铛刷新，但打开的执行详情抽屉不随新写更新 → `useNotificationChanges.ts` 增加 `invalidateQueries(['operation-execution'])`（前缀无参即命中所有详情）。
- 🟡 记录：i18n `operations.paramsPlaceholder` 已被新 `paramTitle`/`paramHint`/JSON placeholder 取代（无引用），删除死代码键。
- 运行态实测通过（R1~R9，证据见 `tmp/运行状态测试方案.md` 第五节/本文件上方会话摘要）：
  - R1 30 并发同幂等 key → 唯一 Execution（DB 计数=1）；R2 触发→Execution+Topic+通知落库时序正确；R7 参数 KV `{"region":"cn","force":true}` 紧凑注入通知可见。
  - R4 删除单条执行 → run 级联删除（DB `operation_run` 0 行）+ 详情 404 + 关联 Topic 保留；R5 清空 → executions=`[]` 但 21 个 Topic 保留。
  - R3 `MarkReadToLast` 原子推进 `read_seq=4==last_seq`，未读清除（API 层已接入 `prov.MarkRead→store.MarkReadToLast`）。
  - R6 服务阶段响应（临时 `op-responder` 热加载）：check-software → global+service 两阶段均 success，运行日志实证 `SUPD_SERVICE=web-demo / SUPD_OPERATION_PHASE=service / SUPD_OPERATION=check-software / SUPD_OPERATION_PARAMS={"who":"r6"}`（验证新增的服务作用域注入，Skill §05 断言成立）。
  - R9 服务运行态：4 autostart 服务全 ready；`warnings:[]` 已序列化空数组（R2-17 修复在实例上成立）。
- **验证**：go build/vet/test（14 包全绿）/pnpm build 通过；二进制重编译嵌入；测试服务 http://localhost:8080 运行中（background job `job-0eb80b16674d47dd886a702f503bb0b5`，`SUPD_LOG_DIR=/tmp/supd-logs`，test_workdir 新增临时 `op-responder` 服务响应者扩展）。
- 遗留：上述 2 处修复（R8 + 死代码清理）、运行态证据与临时扩展未提交。

---

## 第六轮全面审计与实例运行状态实测（缺陷修复与最终验收）

- **触发原因**：按 `tmp/audit_plan.md` 对操作中心与通知中心两大新增功能、需求规格一致性及 Skill 有效性进行终审，自行评估最佳修复方案，形成严谨的运行状态测试方案并在真实运行实例中执行测试与留存服务。
- **审计发现与 3 处缺陷修复**：
  1. 🟠 **Issue 1（操作历史表格失败状态误显"成功"）**：`web/src/pages/Operations.tsx` 执行历史表格仅判断 `ex.finished_at ? t.operations.finishedSuccess : t.operations.running`，未检查 run 失败状态，导致失败执行错误展示绿色"成功"与 CheckCircle 图标。修复：引入 `hasFailed` 检查，当 finished 且存在失败 run 时正确呈现 `finishedFailed`（失败）红色 danger 徽章及 `XCircle` 图标。
  2. 🟡 **Issue 2（通知中心 URL Query 参数残留）**：`web/src/pages/Notifications.tsx` 在用户折叠详情、删除单项或清空全部时未同步清除 URL 栏的 `?topic=<id>` 参数，导致刷新或后退仍旧定位旧/已删主题。修复：在 `handleExpand` 折叠、`handleDelete` 及 `handleClearAll` 中加入 `if (urlTopic) navigate('/notifications', { replace: true })`。
  3. 🟡 **Issue 3（扩展配置 OperationsTagInput React Key 冲突）**：`web/src/components/extension/OperationsTagInput.tsx` 使用 `key={op}` 导致重复标签出现 key warning。修复：改为 `${op}-${index}` 复合 key 并按 index 过滤删除。
- **修改文件清单**：
  - `web/src/pages/Operations.tsx`
  - `web/src/pages/Notifications.tsx`
  - `web/src/components/extension/OperationsTagInput.tsx`
  - `test_workdir/extensions/operation-global-ext/meta.yaml`（新增 `fail-demo` 失败测试操作）
  - `test_workdir/extensions/operation-global-ext/run.sh`（增加 `fail-demo` 退出码 2 分支）
  - `tmp/运行状态测试方案.md`（完整 7 项实例测试用例与执行记录）
- **自动化测试验证**：
  - `go build ./...`、`go vet ./...`、`go test ./... -count=1`（14 个包全部 PASS）。
  - `go test -race ./internal/api ./internal/store ./internal/extension`（高并发竞争检测全部 PASS，0 race）。
  - `cd web && pnpm build`（TypeScript 严格编译与 Vite 构建通过，0 错误）。
  - `go build -o supd ./cmd/supd`（嵌入最新构建产物）。
  - `validate_dev.py` 对示例 11、示例 12 及 test_workdir 扫描（全部 0 error, 0 warning）。
- **实例运行状态测试（TC-01 ~ TC-07 真实实机实测全部 PASS）**：
  - **TC-01（DB & WAL）**：SQLite WAL 模式及 4 张核心表结构就绪。
  - **TC-02（并发幂等）**：30 并发高压触发相同 `Idempotency-Key`，全部返回 HTTP 200 且返回同一 `execution_id`，SQLite 中严格仅插入 1 条记录。
  - **TC-03（失败状态修复实测）**：触发 `fail-demo` 操作返回 exit code 2，后端记录 `state: "failed"`，前端表格准确展示红色"失败"徽标。
  - **TC-04（通知行协议与原子游标）**：行协议生成通知，调用 `/read` 推进原子游标至 `read_seq == last_seq == 2`，未读红点归零。
  - **TC-07（Skill 工具脚本）**：`validate_dev.py` 严格校验通过。
- **服务状态**：测试服务持续运行于 `http://localhost:8080`（工作目录 `test_workdir`），可直接供用户访问检查。

---

## 最终审计与 v0.1.1 发布

- **触发原因**：完成全部审计修复后，执行最终审计与测试；确认无问题后升级版本号并推送，结束本功能发版闭环。
- **最终质量门禁（全绿）**：
  - `go build ./...` ✅、`go vet ./...` ✅（0 警告）。
  - `go test ./... -count=1` ✅（14 个包全部 PASS）。
  - `go test -race ./internal/api ./internal/store ./internal/extension ./internal/notification ./internal/stream ./internal/logging` ✅（0 race）。
  - `cd web && pnpm build` ✅（TypeScript 严格编译 + Vite 构建成功，生成 Operations/Notifications/octagon-alert chunk）。
  - 版本注入：`go build -ldflags "-X main.version=0.1.1" -o /tmp/supd-test ./cmd/supd/` → `supd 0.1.1` ✅。
  - `validate_dev.py` 对 skill 下 12 个示例逐一校验 → 全部 0 error 0 warning。
- **最终审计抽查（断言属实证）**：确认关键修复已在代码中真实落地——
  - `MarkReadToLast` 原子推进命令（`internal/store/commands.go:393`）。
  - 幂等 `idempotentPut`（互斥锁 + 过期清理，修复上轮被误改为无锁直写）。
  - `pruneExecutions` 改 `ORDER BY created_at DESC`（`internal/store/retention.go:95`），execution 查询同样 `ORDER BY created_at DESC`。
  - DB 迁移实测仅到 v2（修正变更记录中误写的 v3）。
- **版本升级 v0.1.1（PATCH）**：本次全部为操作中心/通知中心缺陷修复，符合 semver PATCH。修改：
  - `README.md`：镜像拉取示例 2 处（v0.1.1 / v0.1.1-debian）+ 项目状态表"当前版本"1 处。
  - `docs/devlog/version-upgrade-guide.md`：变更记录追加 v0.1.1 行。
  - `docs/devlog/session-notes.md`：顶部当前版本、会话历史索引表追加本行、最近会话重点更新。
- **发布动作**：提交全部变更 → annotated tag `v0.1.1` → `git push origin main && git push origin v0.1.1`（含此前待推送的 v0.1.0 提交 eb7e13b/f54a8d0/9a90c18 与 tag v0.1.0，触发 CI 构建双架构镜像与 Release）。
- **遗留**：远程推送若因网络不可达再次失败，需在账单内重跑 `git push origin main && git push origin v0.1.0 && git push origin v0.1.1`。
