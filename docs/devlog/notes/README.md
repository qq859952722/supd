# devlog/notes 子文件夹说明

> 本目录存放按需读取的详细备忘。主索引在 `../session-notes.md`。
> **记录原则（用户决定 2026-09-01）**：开发日志只记录 supd 仓库源码/文档调整；对远程服务器（188/190）的运维操作（服务部署、目录调整、SSH 调试、配置修复等）不写入日志。

## 读取协议

1. **新会话启动**：只读 `../session-notes.md`（主索引）+ `../blockers.md`，**不要默认读取本目录文件**
2. **按需读取**：历史会话详情在 `../archive/notes-20260721-20260901-source.md`（压缩归档，仅源码相关内容）
3. **搜索优先**：如需查找特定主题，用 `rg` 搜索归档文件关键词，定位后再读

## 文件清单

| 文件 | 内容 | 何时读取 |
|------|------|----------|
| `core-mechanisms.md` | 核心机制详细备忘（生命周期/扩展/环境变量/身份/关机/PID1/watcher/API/前端/Docker/CI） | 涉及底层机制、排查行为是否符合设计时 |

## 会话日志归档规则（2026-09-01 起）

每次会话结束后：
1. 会话涉及 **supd 仓库源码/文档调整** → 将完成要点压缩追加到 `../archive/notes-20260721-20260901-source.md` 对应日期段落（新日期追加新 `## YYYY-MM-DD` 段落），并同步主索引
2. 会话仅为**远程服务器运维**（188/190 服务调整等）→ 不写入开发日志
3. 主索引 `../session-notes.md` 更新"最近会话重点"与索引表（仅源码相关内容）
