# 2026-07-29 开发日志

## 会话主题：supd 启动信息摘要（Startup Banner）运行测试 + v0.0.39 发布

### 触发原因

延续 2026-07-28 启动信息摘要开发，本轮完成：扩展阻塞修复、代码审计、运行状态测试、版本升级与 GitHub 推送。

### 完成要点

1. **post_ready 扩展异步执行修复**
   - 问题：`supdLifecycleTrigger.OnPostReady(ctx)` 同步调用，post_ready 扩展（supd-startup-hook 的 notify action，约 3 秒）阻塞主 goroutine，导致启动摘要第二段（HTTP 实际监听地址）与"supd 运行中"提示延迟可见。
   - 修复：将 `OnPostReady` 从 HTTP 服务器启动前移至 HTTP goroutine 启动后，并用 `go` 异步执行。
   - 规格 §2.8.3 要求 Step 8（HTTP）在 Step 11（post_ready）之前，异步执行满足此顺序；dispatcher/taskRecorder/TaskHistory 均有 mutex 保护，线程安全。

2. **运行状态测试（端到端验证）**
   - 启动摘要第一阶段（Bootstrap 后静态配置）✅
   - 启动摘要第二阶段（HTTP 绑定后实际监听地址 + 可访问 URL 枚举）✅
   - IPv4+IPv6 双栈地址枚举（192.168.31.184 / 127.0.0.1 / 2408:... / ::1）✅
   - post_ready 异步执行：HTTP 监听 08:37:26.601 → post_ready 开始 08:37:26.603（仅晚 2ms，不阻塞）✅
   - pre_start 钩子执行 ✅
   - pre_shutdown 钩子执行（关机时触发）✅
   - API `/api/system/status` 返回正确（http_listen/version/uptime）✅
   - API `/api/services` 返回 9 个服务 ✅
   - 优雅关闭（SIGTERM）exit code 0 ✅
   - 关机等待扩展任务结束（"等待运行中的扩展任务结束..."）✅

3. **完整回归**
   - `go build ./...` ✅、`go vet ./...` ✅、`go test ./... -count=1` 全绿（core 51.9s、extension 34.3s）
   - `cd web && pnpm build` ✅（19.07s）

4. **版本升级 v0.0.39**
   - 提交：`754600d` fix(cli): post_ready 异步、`7e26d68` release: v0.0.39
   - 标签 `v0.0.39` 创建并推送 GitHub，CI 自动 Release 构建已触发
   - README.md（docker pull tag + 当前版本）、version-upgrade-guide.md 更新

### 修改文件清单

| 文件 | 变更 |
|------|------|
| `internal/cli/run.go` | post_ready 从同步改为异步 `go supdLifecycleTrigger.OnPostReady(ctx)`，移至 HTTP goroutine 之后 |
| `README.md` | v0.0.38 → v0.0.39（docker pull + 当前版本） |
| `docs/devlog/version-upgrade-guide.md` | 新增 v0.0.39 变更记录 |

### 关键技术点

- **dispatcher 不记录成功扩展运行**：supd.log 仅在扩展失败时记 ERROR/WARN；成功运行仅写入 `/tmp/supd-logs/extensions/<ext>/<run_id>.log`。排查 post_ready 是否执行需查扩展运行日志文件，而非 supd.log。
- **post_ready 异步时序**：HTTP 服务器在 goroutine 中 `net.Listen` 后立即回调 `addrReady` 打印第二段摘要，主 goroutine 同时启动 post_ready goroutine。由于 `net.Listen` 绑定极快（~1ms），两段摘要几乎同时出现，post_ready 的 3 秒执行完全在后台。
- **关机时仍等待异步 post_ready**：`dispatcher.GetConcurrencyManager().HasAnyRunning()` + `WaitForAllRunning` 确保关机前等待 post_ready 完成，再执行 GracefulShutdown（含 pre_shutdown 钩子）。
- **v0.0.38 网络阻断已恢复**：上轮 v0.0.38 标签因网络超时未推送，本轮确认远程已有 v0.0.38 标签，阻断自动解除。

### 遗留事项

- CI 构建结果待确认（v0.0.39 Release 工作流 in_progress）
- 启动摘要第二段（实际监听地址）偶尔在"supd 运行中"提示之后打印（goroutine 调度时序，功能正确，UX 可接受）
