# 2026-08-02

## Skill 配置结构与开发工具完善

### 触发原因

用户要求完善、修复 `supd-service-extension-dev` Skill，并要求所有修复与项目源代码相互印证。

### 完成要点

- 补充 `<baseDir>`、服务、全局扩展、服务级扩展和 runtime 的统一目录契约，明确发现路径、watcher 边界及运行期写入边界。
- 修正扩展入口契约：`entry` 必填、`runtime` 可选；runtime 非空时由解释器执行，空时入口需要执行权限；补充 timeout 默认值与硬上限语义。
- 区分服务与扩展环境变量来源：服务使用 `config.yaml.env_files`，扩展按顺序扫描全局及关联目录的 `env.yaml`/`env/*.yaml|yml`。
- 补全 package profile 名称约束、默认 profile 三级回退、强制包含及强制排除规则。
- 修复 `validate_dev.py` 的错误退出契约：所有 FAIL 累计并返回非零；严格校验 `bin/`、根目录散落文件、直接执行权限，并递归校验服务级扩展。
- 修复 `pack_dev.py` 默认导出规则解析，支持 `package.default.yaml` → `service.yaml package` → 内置规则回退，并拒绝非法 profile 名。
- 补齐简单服务的 env、profile 和服务级扩展示例；补齐复杂服务实际可运行的 `bin/myapp`。
- 对照需求规格 §2.3.2 确认服务 version 必须为三段数字；记录当前 Go `ValidateService` 仅检查非空，Skill 开发校验器负责补齐规格门禁，避免错误声称已与 Go 服务校验完全一致。

### 源码印证

- 目录发现与 env 文件：`internal/watch/discovery.go`
- watcher 白名单与跳过目录：`internal/watch/watcher.go`
- 服务/扩展校验及默认值：`internal/config/service_validate.go`、`extension_validate.go`、`extension.go`
- 环境变量合并：`internal/core/service_env.go`、`internal/extension/dispatcher.go`、`internal/config/env_merge.go`
- profile 解析与打包规则：`internal/config/package_profile.go`、`internal/archive/packer.go`
- config.yaml 初始化和启动加载：`internal/cli/init.go`、`internal/cli/run.go`

### 修改文件

- `.trae/skills/supd-service-extension-dev/SKILL.md`
- `.trae/skills/supd-service-extension-dev/references/01_service_spec.md`
- `.trae/skills/supd-service-extension-dev/references/02_extension_spec.md`
- `.trae/skills/supd-service-extension-dev/references/05_env_spec.md`
- `.trae/skills/supd-service-extension-dev/scripts/validate_dev.py`
- `.trae/skills/supd-service-extension-dev/scripts/pack_dev.py`
- `.trae/skills/supd-service-extension-dev/examples/README.md`
- `.trae/skills/supd-service-extension-dev/examples/01-simple-service/README.md`
- `.trae/skills/supd-service-extension-dev/examples/01-simple-service/env.yaml`
- `.trae/skills/supd-service-extension-dev/examples/01-simple-service/package.default.yaml`
- `.trae/skills/supd-service-extension-dev/examples/01-simple-service/package.migrate.yaml`
- `.trae/skills/supd-service-extension-dev/examples/01-simple-service/extensions/health-action/meta.yaml`
- `.trae/skills/supd-service-extension-dev/examples/01-simple-service/extensions/health-action/run.sh`
- `.trae/skills/supd-service-extension-dev/examples/02-complex-service/bin/myapp`

### 验证

- Skill 两个服务示例、八个独立扩展示例及嵌套服务级扩展正例通过。
- 目录名与配置名不匹配负例返回非零。
- 非法 profile `../bad` 返回非零。
- 默认 profile 与 `migrate` profile 打包验证通过。
- Python `py_compile` 通过，生成的 `__pycache__` 已清理。
- `git diff --check` 通过。
- `go build ./...`、`go vet ./...`、`go test ./... -count=1` 全部通过。

### 遗留事项

- Go `ValidateService` 尚未执行已定义的 `versionRegex`。本次任务仅修复 Skill，未修改 Go 源码；Skill 已明确该实现缺口并按需求规格严格校验。
