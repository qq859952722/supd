# 2026-08-31 会话

## tjs Release 缓存复用验证（v0.0.54）

- 回查确认 v0.0.52 未复用 tjs 属修复前历史行为：运行时 Release 中无正确命名资产，四个 tjs job 均实际执行编译，上传被 `source#label` 语法存成同名 `tjs`（08:56:33）。
- 四个正确命名资产（alpine/debian × amd64/arm64，`v26.6.0-v3`）已由 v0.0.53 run 写入 `tjs-cache`（14:46–14:47）。
- v0.0.54 为无代码变更的验证性发布：tag 触发 release workflow 后应命中 Release 资产并跳过编译，用于验证完整缓存链路。

## tjs Release 资产命名修复（v0.0.53）

- 发现 `tjs-cache` Release 实际只有名为 `tjs` 的资产，`gh release upload source#label` 只设置显示 label，不会改变资产名称，导致按平台/架构/version 查找始终未命中。
- 两个 workflow 改为先复制为完整目标资产名再上传，并在上传后校验资产确实存在；准备 Release 时清理旧的无平台信息 `tjs` 资产。
- 正式发布和手动构建的上传条件统一为 Release asset 未命中时上传，Actions Cache 命中时也能补齐长期 Release 缓存。
- 未执行本地 Go、前端或 Docker 构建。

## build-push workflow 调度修复（v0.0.52）

- GitHub Actions 最近两次运行在调度前失败，无 job、无日志，workflow 列表显示为 `.github/workflows/build-push.yml`。
- 根因是 `build-alpine` 与 `build-debian` 重复声明 job-level `if`，GitHub Actions 无法解析 workflow。
- 修复为每个 job 只保留一个合并条件，并保留 `needs: ensure-tjs-cache-release`；`push: false` 时跳过 Release 初始化但仍允许构建。
- 未执行本地 Go、前端或 Docker 构建。

## 扩展 CWD/entry 解析根回归修复（v0.0.44 引入）

### 触发原因
用户反馈 192.168.31.190:7979 上**所有扩展启动失败**，报 `ReferenceError: could not load '<服务根>/run.js' - ENOENT`（如 filebrowser-updater）。怀疑是工作目录识别错误，要求先用实例测试，再分代码定位，并顺带分析各服务日志排查其他问题。

### 实例验证
- 触发 `filebrowser-updater` 扩展，日志显示 entry `run.js` 被解析到服务根目录 `/etc/supd/services/filebrowser/run.js`（不存在），而实际脚本位于 `/etc/supd/services/filebrowser/extensions/filebrowser-updater/run.js`。
- 线上 `meta.yaml` 使用相对 `entry: run.js`，运行期解析根错误导致全部扩展启动失败。

### 根因
v0.0.44「统一配置路径解析规则」提交（`12980ff`）修改了 `internal/extension/dispatcher.go` 的 `buildWorkDir`：

```go
// v0.0.44 之前（正确）：返回扩展自身目录
// 注释：工作目录为扩展自身目录（meta.yaml 所在目录），这样 entry 中的相对路径（如 run.sh）可以正确定位
return extDir

// v0.0.44（回归）：全局返回 baseDir，服务级返回服务根目录
if extEntry.ServiceName != "" {
    return filepath.Join(baseDir, "services", extEntry.ServiceName)
}
return baseDir
```

导致扩展进程 CWD 与相对 entry 的解析根从**扩展自身目录**错误改为**服务根/baseDir**。

### 修复
- `internal/extension/dispatcher.go`：`buildWorkDir` 恢复返回 `filepath.Dir(extEntry.ConfigPath)`（扩展自身目录）；script_tmp 仍创建供临时文件（不自动清理）
- `internal/api/extension_provider.go`：`RunExtension` 的 workDir 改为扩展目录；serviceDir 单独计算仅用于 `SUPD_SERVICE_DIR` 注入
- `internal/api/extension_handler.go`：导出/导入校验 `validateExtensionForExport` 的 entryRoot 改为扩展自身目录；`validateExtensionImport` 去掉 entryRoot 参数
- `internal/api/export_handler.go`：服务包内扩展校验的 entry 解析根改为扩展目录
- `internal/extension/run_context.go`：`WorkDir` 字段注释同步

### 文档同步
- `docs/需求规格说明_v1.5.md`：§2.2.3 entry 示例/注释、§2.2.12 CWD 与 script_tmp 区分、§2.5.6 entry 解析规则、§2.12.3 打包校验、词汇表 Entry
- `.trae/skills/supd-service-extension-dev/SKILL.md`：路径基准说明
- `.trae/skills/supd-service-extension-dev/references/02_extension_spec.md`、`05_env_spec.md`
- `.trae/skills/supd-service-extension-dev/scripts/validate_dev.py`：entry 相对扩展目录解析
- 6+ 个扩展示例 `meta.yaml`（entry 改相对路径）与 README

### 测试
- 更新 `internal/extension/dispatcher_test.go` 期望（workDir 期望改为 extDir）
- `go build ./...` / `go vet ./...` / `go test ./... -count=1` 全通过
- 所有示例扩展通过 `validate_dev.py` 校验

### 其他发现（日志分析）
- **code-server 宕机**：`argon2.node` 依赖 glibc（`ld-linux-x86-64.so.2`），当前 Node 为 musl 版本，不兼容导致启动失败。用户要求后续单独处理，未纳入本次修复。
- 其他服务仅有非致命警告（如 adguardhome 过滤器更新失败、dnscrypt-proxy UDP 缓冲偏小）。
- `SUPD_SCRIPT_TMP` 在 Skill 文档中提及但**代码从未注入**（规格 §2.2.5 变量表亦不含此变量），且无示例使用——预存 Skill 文档/代码偏差。已修正 Skill 文档两处（`SKILL.md`、`references/02_extension_spec.md`），改为直接写 `<baseDir>/script_tmp/<dir>` 绝对路径，不再声称存在该环境变量。

### 遗留
- 待构建并部署到 190 验证扩展恢复
- code-server argon2 glibc/musl 兼容性问题单独处理

## 镜像层增量更新优化（v0.0.48）

### 触发原因
用户希望更新镜像时尽量只拉取变化的 supd 二进制，减少镜像更新流量。

### 修改
- `Dockerfile` 与 `Dockerfile.debian` 将 tjs 包装脚本、supd 二进制和 tjs 二进制拆分为独立构建层。
- 使用 `COPY --link --chmod=755` 复制 supd/tjs，更新 supd 时最大化复用基础系统、tjs 和包装脚本层。
- 保持现有 GitHub Actions 的 `context: .`、`target: runtime`、Buildx 和 registry cache 配置不变；CI 下载的 `./tjs` 路径不变。

### CI 兼容性审查
- `docker/build-push-action@v7` 使用 BuildKit，支持 `COPY --link` 和 `--chmod`。
- Alpine 与 Debian 两条镜像 workflow 的构建上下文、Dockerfile、架构矩阵、tjs artifact 和镜像 target 均与修改后 Dockerfile 对齐。
- 按用户要求未执行本地构建，仅完成 workflow 与 Dockerfile 静态兼容性审查。

## tjs 编译缓存增强

- 正式发布和手动构建 workflow 的 tjs 缓存 key 增加缓存 schema、基础镜像/ libc、架构和 `TJS_VERSION` 信息。
- 同一源版本且构建环境不变时复用 `/tmp/tjs-binary`；编译流程、补丁或基础环境变更时递增 `TJS_CACHE_SCHEMA` 自动失效，避免复用不兼容产物。

## tjs 固定 Release 缓存（v0.0.50）

- 正式发布和手动镜像 workflow 共用固定的 `tjs-cache` prerelease，不创建每个 tjs 版本的独立 Release。
- 缓存命中顺序为 Actions Cache → `tjs-cache` Release asset → 容器内编译；命中后仍写入 `/tmp/tjs-binary/tjs`，不改变原有 artifact 和 Docker 构建流程。
- asset 按 Alpine/Debian、amd64/arm64、`TJS_VERSION` 和 `TJS_CACHE_SCHEMA` 区分；清理任务按版本保留最近 5 个版本的全套资产，并通过 workflow 并发组避免上传/清理竞争。
