# supd 需求偏差记录

> 记录实现与需求原文不一致的情况，供人工确认。
> 当前状态：无活动偏差。下表为"已确认"偏差台账（DEV-009 / DEV-010 为节点 01 同步新增，其余历史编号见 `AGENTS.md` 第五节；已修复或已转规格的历史条目不在此列）。

---

## 已确认偏差

| 编号 | 描述 | 决策来源 | 状态 |
|---|---|---|---|
| **DEV-009** | 数据库约束变更：解除"禁止引入数据库"约束，改为仅允许 SQLite（`modernc.org/sqlite` 纯 Go）用于通知中心/操作中心持久化（`<baseDir>/data/supd.db`，**不开放脚本访问**）；仍禁止其他数据库（Bolt/Badger 等）与 SSE/WebSocket | 设计稿 v5 + 用户确认 | 已确认 |
| **DEV-010** | 导航变更：顶部导航 7 Tab → 8 Tab（新增"操作"Tab，`/operations`，与定时任务同级）+ 顶栏通知铃铛（位于全局搜索与用户菜单之间，仅红点无数字，保持"不使用徽标数字"约束）；操作中心与通知中心为两个独立入口/页面 | 设计稿用户确认项 | 已确认 |
